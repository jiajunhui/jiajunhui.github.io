<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[播放器基础框架PlayerBase]]></title>
    <url>%2Fproject_playerbase%2F</url>
    <content type="text"><![CDATA[2018/06/04日更新 与掘金博文同步：Android播放器基础封装库PlayerBase 简述视频播放是我们开发中比较常见的场景。这两年关于视频方面的热度不断提升，可以说前两年是直播年，今年是小视频年，各种短视频应用铺天盖地。对于视频的业务场景也越来越丰富，功能也越来越多。对于我们开发来说播放相关组件的代码变得也越来越复杂，管理维护成本也越来越高，面对不断迭代的业务，我们需要一种有效的方案来应对这种频繁的业务变化。 这几年一直在做视频相关的业务，手机端和TV端均做过适配开发。MediaPlayer、exoplayer、ijkplayer、VLC、FFmpeg等都摸索使用过。这一路遇到很多问题……说多了都是泪，为了适应多变的产品需求，中间重构了N多个版本。最终PlayerBase也就诞生了。PlayerBase3 版本进行了完整重构设计，目前大致框架基本已稳定下来。对于大部分应用视频播放组件场景都能轻松处理。 项目地址：https://github.com/jiajunhui/PlayerBase 本文目录 框架简介 功能特色 部分使用示例 框架的设计 功能使用 P图技术有限，文中图片就凑合着看吧！ 框架简介请注意！ 请注意！ 请注意！ PlayerBase区别于大部分播放器封装库。 PlayerBase是一种将解码器和播放视图组件化处理的解决方案框架。您需要什么解码器实现框架定义的抽象引入即可，对于视图，无论是播放器内的控制视图还是业务视图，均可以做到组件化处理。将播放器的开发变得清晰简单，更利于产品的迭代。 PlayerBase不会为您做任何多余的功能业务组件，有别于大部分播放器封装库的通过配置或者继承然后重写然后定制你需要的功能组件和屏蔽你不需要的功能组件（这种之前我也经历过，上层可能需要经常改动，感觉很low!!!）。正确的方向应该是需要什么组件就拓展添加什么组件，不需要时移除即可，而不是已经提供了该组件去选择用不用。 功能特色 视图的组件化处理 视图组件的高复用、低耦合 解码方案的组件化、配置化管理 视图组件的完全定制 视图组件的热插拔，用时添加不用时移除 自定义接入各种解码方案 解码方案的切换 支持倍速播放 支持Window模式播放 支持Window模式的无缝续播 支持列表模式的无缝续播 支持跨页面无缝续播 支持调整画面显示比例 支持动态调整渲染视图类型 支持VideoView切角处理，边缘阴影效果 提供自定义数据提供者 统一的事件下发机制 扩展事件的添加 等功能…… 部分使用示例 解码配置和框架初始化 123456789101112131415public class App extends Application &#123; @Override public void onCreate() &#123; //... //如果您想使用默认的网络状态事件生产者，请添加此行配置。 //并需要添加权限 android.permission.ACCESS_NETWORK_STATE PlayerConfig.setUseDefaultNetworkEventProducer(true); //设置默认解码器 int defaultPlanId = 1; PlayerConfig.addDecoderPlan(new DecoderPlan(defaultPlanId, IjkPlayer.class.getName(), "IjkPlayer"));PlayerConfig.setDefaultPlanId(defaultPlanId); //初始化库 PlayerLibrary.init(this); &#125;&#125; 组装组件（添加您需要的组件【组件来自用户自定义，框架不提供任何视图组件】） 123456789ReceiverGroup receiverGroup = new ReceiverGroup();//Loading组件receiverGroup.addReceiver(KEY_LOADING_COVER, new LoadingCover(context));//Controller组件receiverGroup.addReceiver(KEY_CONTROLLER_COVER, new ControllerCover(context));//CompleteCover组件receiverGroup.addReceiver(KEY_COMPLETE_COVER, new CompleteCover(context));//Error组件receiverGroup.addReceiver(KEY_ERROR_COVER, new ErrorCover(context)); 设置组件启动播放 12345BaseVideoView videoView = findViewById(R.id.videoView);videoView.setReceiverGroup(receiverGroup);DataSource data = new DataSource("http://url...");videoView.setDataSource(data);videoView.start(); 事件的监听 123456789101112131415//player eventvideoView.setOnPlayerEventListener(new OnPlayerEventListener()&#123; @Override public void onPlayerEvent(int eventCode, Bundle bundle)&#123; //... &#125;&#125;);//receiver eventvideoView.setOnReceiverEventListener(new OnReceiverEventListener()&#123; @Override public void onReceiverEvent(int eventCode, Bundle bundle) &#123; //... &#125;&#125;); 详细使用示例请参阅github项目主页介绍 框架的设计视图的处理别小看一个小小的播放器，里面真的是别有洞天。有时视图组件复杂到你怀疑人生。 我们先看下播放器开发时常见的一些视图场景： 以上是我们最常见到的一些视图（其实还有很多，比如清晰度切换、视频列表、播放完成提示页等等），这些视图如果没有一个行之有效的方案来进行管理，将逐渐会乱到失控。 上面只是列出了控制器视图、加载视图、手势视图、错误视图、弹幕视图和广告视图，这一股脑的视图都是和播放紧密相连的，完全由播放状态驱动，视图之间可能共存、可能制约。那么这些视图如何进行统一的管理呢？要是一股脑全写到一个xml中，想想都可怕……，好点的可能封装成一个一个的组合控件View或者用ids进行绑定管理，但是播放器和组件间的通信、组件与组件间的通信是个问题。且看PlayerBase如何做。 接收者Receiver与覆盖层Cover的概念做过播放器开发的应该都很清楚一点，所有视图的工作都是由状态事件来驱动的，这是一条主线。有可能是来自播放器的事件（比如解码器出错了），也有可能是来自某个视图的事件（比如手势调节播放进度），还有可能是外部事件（比如网络状态变化）。 这些信息我们可以归结为 视图是事件接收者，也是事件的生产者 解码器是事件生产者 可能有外来的事件生产者 也就是说我们把视图当做事件接收者，同时视图具备发送事件的能力。 解码器不断发出自己工作状态的事件要传递给视图。 外部的某些事件也需要传递给视图 至此，框架内部定义了事件接收者的概念，接收者作为事件消费者的同时也能生产事件，而覆盖层继承自接收者引入了视图View。 12345678910111213141516171819202122232425262728public abstract class BaseReceiver implements IReceiver &#123; //... protected final void notifyReceiverEvent(int eventCode, Bundle bundle)&#123; //.. &#125; /** * all player event dispatch by this method. */ void onPlayerEvent(int eventCode, Bundle bundle); /** * error event. */ void onErrorEvent(int eventCode, Bundle bundle); /** * receivers event. */ void onReceiverEvent(int eventCode, Bundle bundle); /** * you can call this method dispatch private event for a receiver. * * @return Bundle Return value after the receiver's response, nullable. */ @Nullable Bundle onPrivateEvent(int eventCode, Bundle bundle); &#125; 12345public abstract class BaseCover extends BaseReceiver&#123; //... public abstract View onCreateCoverView(Context context); //...&#125; 且看代码，有播放器的事件、有错误事件、有组件（Receiver）间的事件。这众多事件如何下发呢，如果有N多个接收者呢，如何破？ 接收者组管理（ReceiverGroup）ReceiverGroup的出现目的就是对众多接收者进行统一的管理，统一的事件下发，当然还有下面的数据共享问题。来张图： 在ReceiverGroup中包含Cover（其实也是Receiver）和Receiver，提供了Receiver的添加、移除、遍历、销毁等操作。当有事件需要下发时，便可通过ReceiverGroup进行统一的遍历下发。 1234567891011121314151617181920212223242526272829303132333435363738394041public interface IReceiverGroup &#123; void setOnReceiverGroupChangeListener(OnReceiverGroupChangeListener onReceiverGroupChangeListener); /** * add a receiver, you need put a unique key for this receiver. * @param key * @param receiver */ void addReceiver(String key, IReceiver receiver); /** * remove a receiver by key. * @param key */ void removeReceiver(String key); /** * loop all receivers * @param onLoopListener */ void forEach(OnLoopListener onLoopListener); /** * loop all receivers by a receiver filter. * @param filter * @param onLoopListener */ void forEach(OnReceiverFilter filter, OnLoopListener onLoopListener); /** * get receiver by key. * @param key * @param &lt;T&gt; * @return */ &lt;T extends IReceiver&gt; T getReceiver(String key); /** * get the ReceiverGroup group value. * @return */ GroupValue getGroupValue(); /** * clean receivers. */ void clearReceivers();&#125; 组件间数据共享（GroupValue）播放器开发中很多时候我们需要依据某个视图的状态来限制另外视图的功能或状态，比如当处于加载中时禁止拖动进度条或者播放出错显示error后禁止其他视图操作等等。这些都属于状态上的相互制约。 GroupValue就相当于提供了一个共享的数据池，当某个数据被刷新时，监听该数据的回调接口能及时收到通知，当然也可以直接去主动获取数据状态。你可以指定你要监听那些数据的更新事件，如果您注册了您要监听的数据的key值，其对应的value被更新时，您就会收到回调。然后您可以在回调中进行UI视图的控制。 1234567891011121314151617181920212223242526public class CustomCover extends BaseCover&#123; //... @Override public void onReceiverBind() &#123; super.onReceiverBind(); getGroupValue().registerOnGroupValueUpdateListener(mOnGroupValueUpdateListener); &#125; //... private IReceiverGroup.OnGroupValueUpdateListener mOnGroupValueUpdateListener = new IReceiverGroup.OnGroupValueUpdateListener() &#123; @Override public String[] filterKeys() &#123; return new String[]&#123; DataInter.Key.KEY_COMPLETE_SHOW &#125;; &#125; @Override public void onValueUpdate(String key, Object value) &#123; //... &#125; &#125;; //... @Override public void onReceiverUnBind() &#123; super.onReceiverUnBind(); getGroupValue().unregisterOnGroupValueUpdateListener(mOnGroupValueUpdateListener); &#125;&#125; 视图的布局管理（分级填充）上文中常见的视图组件，我们在使用中肯定会遇到覆盖优先级的问题。举个栗子，比如Error视图出现后其他的视图一概不可见，也就是说Error视图的优先级是最高的，谁都不能挡着它，我们创建了一个个的Cover视图，对于视图的放置就需要一个视图的优先级标量（CoverLevel）来进行控制，不同的Level的Cover视图会被放置于不同级别的容器内。 总结为以下： 指定Cover的优先级CoverLevel Cover组件被添加时自动根据Level值进行分别放置 示意图代码示例 12345678public class CustomCover extends BaseCover&#123; //... @Override public int getCoverLevel() &#123; return ICover.COVER_LEVEL_LOW; &#125; //...&#125; 默认的视图容器管理器 12345678910111213141516171819public class DefaultLevelCoverContainer extends BaseLevelCoverContainer &#123; //... @Override protected void onAvailableCoverAdd(BaseCover cover) &#123; super.onAvailableCoverAdd(cover); switch (cover.getCoverLevel())&#123; case ICover.COVER_LEVEL_LOW: mLevelLowCoverContainer.addView(cover.getView(),getNewMatchLayoutParams()); break; case ICover.COVER_LEVEL_MEDIUM: mLevelMediumCoverContainer.addView(cover.getView(),getNewMatchLayoutParams()); break; case ICover.COVER_LEVEL_HIGH: mLevelHighCoverContainer.addView(cover.getView(),getNewMatchLayoutParams()); break; &#125; &#125; //...&#125; 组件的层级关系如图： 事件生产者（EventProducer）顾名思义，就是它是产生事件的源。比如系统网络状态发生了变化，发出了通知，然后各个应用根据自己的情况来调整显示或设置等。又或者电池电量的变化和低电量预警通知事件等。 再比如，我们上文中的弹幕视图中需要显示弹幕数据，弹幕数据来自服务器，我们需要源源不断的从服务器上取数据，然后显示在弹幕视图。取回数据传给视图的这个过程我们可以将其看作是一个事件生产者在不断生产弹幕数据更新事件，弹幕数据更新时不断将事件发送给弹幕视图来刷新显示。 框架内自带了一个网络变化事件生产者的示例： 123456789101112131415161718192021222324252627282930public class NetworkEventProducer extends BaseEventProducer &#123; //... private Handler mHandler = new Handler(Looper.getMainLooper())&#123; @Override public void handleMessage(Message msg) &#123; super.handleMessage(msg); switch (msg.what)&#123; case MSG_CODE_NETWORK_CHANGE: int state = (int) msg.obj; //...将网络状态发送出去 getSender().sendInt(InterKey.KEY_NETWORK_STATE, state); PLog.d(TAG,"onNetworkChange : " + state); break; &#125; &#125; &#125;; //... public NetworkEventProducer(Context context)&#123; //... &#125; //... public static class NetChangeBroadcastReceiver extends BroadcastReceiver &#123; @Override public void onReceive(Context context, Intent intent) &#123; //... //post state message &#125; //... &#125;&#125; 由于事件生产者所发出的事件是针对Receiver的，所以会被回调到onReceiverEvent()中，如下代码： 12345678public class CustomCover extends BaseCover&#123; //... @Override public void onReceiverEvent(int eventCode, Bundle bundle) &#123; //... &#125; //...&#125; 内容提供者（DataProvider）DataProvider是为了播控的统一以及使用上的优雅而设计的。 在开发中，我们可能会遇到如下场景：你拿到的数据源可能只是个id之类的标识，并不是能直接播放的uri或者url，需要你再用这个id去请求一个接口才能拿到播放的源地址。通常我们都是先去请求接口，然后在成功回调中用拿到的源数据再设置给播放器去播放。 DataProvider的设计就是为了将此过程独立出来包装为一个数据提供者（其实也可以叫数据生产者），拿到数据后发送出去即可。而您只需要把那个id标识给DataProvider即可，接下来的过程就由DataProvider来完成了。DataProvider的具体实现需要由用户完成。 12345678910111213141516171819202122232425262728public class MonitorDataProvider extends BaseDataProvider &#123; //... public MonitorDataProvider()&#123; //... &#125; private Handler mHandler = new Handler(Looper.getMainLooper()); @Override public void handleSourceData(DataSource sourceData) &#123; this.mDataSource = sourceData; //...provider start onProviderDataStart(); //... //...将数据回调出去 onProviderMediaDataSuccess(bundle); //... //...异常时 onProviderError(-1, null) &#125; //... @Override public void cancel() &#123; //...cancel something &#125; @Override public void destroy() &#123; //...destroy something &#125;&#125; 注意： 数据提供者必须要设置在启动播放前。 功能使用VideoView的使用大致归结为以下步骤： 初始化VideoView，并设置相应的监听事件或者数据提供者等 使用ReceiverGroup组装需要的组件Cover和Receiver 把组件设置给VideoView 设置数据启动播放 暂停恢复播放等操作 销毁播放器 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657public class VideoViewActivity extends AppCompatActivity implements OnPlayerEventListener&#123; //... BaseVideoView mVideoView; @Override public void onCreate(Bundle saveInstance)&#123; super.onCreate(saveInstance); mVideoView = findViewById(R.id.videoView); mVideoView.setOnPlayerEventListener(this); //设置数据提供者 MonitorDataProvider MonitorDataProvider dataProvider = new MonitorDataProvider(); mVideoView.setDataProvider(dataProvider); //... ReceiverGroup receiverGroup = new ReceiverGroup(); //Loading组件 receiverGroup.addReceiver(KEY_LOADING_COVER, new LoadingCover(context)); //Controller组件 receiverGroup.addReceiver(KEY_CONTROLLER_COVER, new ControllerCover(context)); //CompleteCover组件 receiverGroup.addReceiver(KEY_COMPLETE_COVER, new CompleteCover(context)); //Error组件 receiverGroup.addReceiver(KEY_ERROR_COVER, new ErrorCover(context)); //... DataSource data = new DataSource("monitor_id"); videoView.setDataSource(data); videoView.start(); &#125; //... public void onPlayerEvent(int eventCode, Bundle bundle)&#123; switch (eventCode)&#123; case OnPlayerEventListener.PLAYER_EVENT_ON_VIDEO_RENDER_START: //... break; case OnPlayerEventListener.PLAYER_EVENT_ON_PLAY_COMPLETE: //... break; &#125; &#125; //... @Override public void onPause()&#123; super.onPause(); mVideoView.pause(); //... &#125; @Override public void onResume()&#123; super.onResume(); mVideoView.onResume(); //... &#125; @Override public void onDestroy()&#123; super.onDestroy(); mVideoView.stopPlayback(); //... &#125;&#125; AVPlayer的使用如果您想直接使用AVPlayer自己进行处理播放，那么大致步骤如下： 初始化一个AVPlayer对象。 初始化一个SuperContainer对象，将ReceiverGroup设置到SuperContainer中。 使用SuperContainer设置一个渲染视图Render，然后自己处理RenderCallBack并关联解码器。 12345678910111213141516171819202122232425262728293031323334353637383940414243SuperContainer mSuperContainer = new SuperContainer(context);ReceiverGroup receiverGroup = new ReceiverGroup();//...add some coversreceiverGroup.addReceiver(KEY_LOADING_COVER, new LoadingCover(context));mSuperContainer.setReceiverGroup(receiverGroup);//...final RenderTextureView render = new RenderTextureView(mAppContext);render.setTakeOverSurfaceTexture(true);//....mPlayer.setOnPlayerEventListener(new OnPlayerEventListener() &#123; @Override public void onPlayerEvent(int eventCode, Bundle bundle) &#123; //...此处需要根据事件自行实现一些特定的设置 //...比如视频的尺寸需要传递Render刷新测量或者视频的角度等等 //将事件分发给子视图 mSuperContainer.dispatchPlayEvent(eventCode, bundle); &#125;&#125;);mPlayer.setOnErrorEventListener(new OnErrorEventListener() &#123; @Override public void onErrorEvent(int eventCode, Bundle bundle) &#123; //将事件分发给子视图 mSuperContainer.dispatchErrorEvent(eventCode, bundle); &#125;&#125;);//...render.setRenderCallback(new IRender.IRenderCallback() &#123; @Override public void onSurfaceCreated(IRender.IRenderHolder renderHolder, int width, int height) &#123; mRenderHolder = renderHolder; bindRenderHolder(mRenderHolder); &#125; @Override public void onSurfaceChanged(IRender.IRenderHolder renderHolder, int format, int width, int height) &#123; &#125; @Override public void onSurfaceDestroy(IRender.IRenderHolder renderHolder) &#123; mRenderHolder = null; &#125;&#125;);mSuperContainer.setRenderView(render.getRenderView());mPlayer.setDataSource(dataSource);mPlayer.start(); 如果非必须，请尽量使用框架封装好的BaseVideoView进行播放，框架相对来说处理的比较完善且提供了丰富的回调和定制性。 关联助手的使用（RelationAssist）现在的短视频应用都有这样的场景： 列表中播放 列表跳详情自然过渡无停顿播放 对于第一条在列表中播放，理论上VideoView就能完成，但是VideoView用在列表中量级较重，不太适合。需要一个轻量化处理的方案。 而对于第二条，VideoView就不行了，VideoView是对解码器进行了包装，当跳到下一个页面时，是一个新的页面自然有新的视图，无法使用前一个页面的播放器实例去渲染当前页面播放。 其实对于这种无缝的续播，原理很简单。就是不同的渲染视图使用同一个解码实例即可。可以简单比作一个MediaPlayer去不断设置不同的surface呈现播放。如果自己处理这个过程的话想对比较繁琐，你需要处理Render的回调并关联给解码器，还需要自己处理Render的测量以及显示比例、角度等等问题。 RelationAssist 就是为了简化这个过程而设计的。在不同的页面或视图切换播放时，您只需要提供并传入对应位置的视图容器（ViewGroup类型）即可。内部复杂的设置项和关联由RelationAssist完成。 12345678910111213141516171819202122232425public class TestActivity extends AppcompatActivity&#123; //... RelationAssist mAssist; ViewGroup view2; public void onCreate(Bundle saveInstance)&#123; super.onCreate(saveInstance); //... mAssist = new RelationAssist(this); mAssist.setEventAssistHandler(eventHandler); mReceiverGroup = ReceiverGroupManager.get().getLiteReceiverGroup(this); mAssist.setReceiverGroup(mReceiverGroup); DataSource dataSource = new DataSource(); dataSource.setData("http://..."); dataSource.setTitle("xxx"); mAssist.setDataSource(dataSource); mAssist.attachContainer(mVideoContainer); mAssist.play(); //... switchPlay(view2); &#125; //... private void switchPlay(ViewGroup container)&#123; mAssist.attachContainer(container); &#125;&#125; 如果您想跨页面进行关联，只需要自己将RelationAssist包装为一个单例即可。此处不做代码展示，详细代码可参见github项目demo代码。 事件助手处理器（EventAssistHandler）视图中的一些基本操作，比如暂停播放、重播、重试、恢复播放等等，这些事件最终都要传递给解码器进行相关操作。可能还有用户自定义的事件比如播放下一个或上一个等。 对于基本的操作事件（暂停、恢复、重播等），框架内部可自动完成，而用户自定的事件需要让用户自行处理。框架内部BaseVideoView和RelationAssist均做了EventAssistHandler的对接，使用时需要传入一个可用的事件处理器对象，可根据不同的事件参数进行相应处理。如下代码： 123456789101112mVideoView.setOnVideoViewEventHandler(new OnVideoViewEventHandler()&#123; @Override public void onAssistHandle(BaseVideoView assist, int eventCode, Bundle bundle) &#123; //基本的事件处理已在父类super中完成，如果需要重写，重写相应方法即可。 super.onAssistHandle(assist, eventCode, bundle); switch (eventCode)&#123; case DataInter.Event.EVENT_CODE_REQUEST_NEXT: //...播放下一个 break; &#125; &#125;&#125;); Window模式播放我们有时可能为了不打断用户的浏览需要小窗播放。框架特意设计了window播放的使用。框架提供了两种window相关的组件。 WindowVideoView FloatWindow WindowVideoView使用上几乎和VideoView是一样的，只不过WindowVideoView是以window的形式呈现的。window默认是可以拖动的，如果您不需要，可以禁止，window的每个设置项都有默认值，window的设置示例代码： 123456789101112FloatWindowParams windowParams = new FloatWindowParams();windowParams.setWindowType(WindowManager.LayoutParams.TYPE_TOAST) .setFormat(PixelFormat.RGBA_8888) .setFlag(WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE) .setDefaultAnimation(true) .setX(100) .setY(100) .setWidth(width) .setHeight(height) .setGravity(Gravity.TOP | Gravity.LEFT));mWindowVideoView = new WindowVideoView(this,windowParams);//... 而FloatWindow只是一个悬浮窗View，您可以传入您要显示的布局View。可以用于窗口切换播放时的无缝续播。此处不做代码示例展示。 一些样式设置（StyleSetter）样式的设置是针对 VideoView、WindowVideoView 和 FloatWindow 的。当然框架提供的StyleSetter您也可以用于别处。提供了如下的样式设置： 1234567891011121314151617181920public interface IStyleSetter &#123; //设置圆角 @TargetApi(Build.VERSION_CODES.LOLLIPOP) void setRoundRectShape(float radius); @TargetApi(Build.VERSION_CODES.LOLLIPOP) void setRoundRectShape(Rect rect, float radius); //设置为圆形 @TargetApi(Build.VERSION_CODES.LOLLIPOP) void setOvalRectShape(); @TargetApi(Build.VERSION_CODES.LOLLIPOP) void setOvalRectShape(Rect rect); //清除样式设置 @TargetApi(Build.VERSION_CODES.LOLLIPOP) void clearShapeStyle(); //设置阴影 //注意阴影的设置要求对应的View对象必须要有背景色（不能是TRANSPARENT） //如果您没设置，框架内部会自定设置为黑色 void setElevationShadow(float elevation); void setElevationShadow(int backgroundColor, float elevation);&#125; 解码器的接入框架自带了系统的MediaPlayer的解码实现，项目demo中示例接入了ijkplayer和exoplayer，如果您想接入其他的解码器，请参见示例代码，以下为简单示例，更详细的请参见项目源码。 接入步骤 继承自BaseInternalPlayer 实现定义的抽象方法 配置引入您的解码器 1234567public class XXXPlayer extends BaseInternalPlayer&#123; public IjkPlayer() &#123; //... &#125; //... //implements some abstract methods.&#125; 通过配置设置使用该解码器。 123int planId = 2;PlayerConfig.addDecoderPlan(new DecoderPlan(planId, XXXPlayer.class.getName(), "XXXPlayer"));PlayerConfig.setDefaultPlanId(planId); 以上对于PlayerBase的讲解基本完成。码字好累！！！ 主要的模块差不多就这么多了，更详细的可参见项目源码。 如有问题联系：junhui_jia@163.com 交流QQ群：600201778 最后再附上项目地址：PlayerBase]]></content>
      <categories>
        <category>我的开源项目</category>
      </categories>
      <tags>
        <tag>Player</tag>
        <tag>Decode</tag>
        <tag>Cover</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Java虚拟机（JVM）介绍及其内存模型]]></title>
    <url>%2Fjava_jvm%2F</url>
    <content type="text"><![CDATA[概述Java虚拟机在执行Java程序的过程中会把它所管理的内存划分为若干个不同的数据区域。这些区域都有各自的用途，以及创建和销毁的时间，有的区域随着虚拟机进程的启动而存在，有些区域则依赖用户线程的启动和结束而建立和销毁。根据《Java虚拟机规范（Java SE 7版）》的规定，Java虚拟机所管理的内存将会包括以下几个运行时数据区域，如图所示： 运行时数据区讲解程序计数器程序计数器（Program Counter Register）是一块较小的内存空间，它可以看作是当前线程所执行的字节码的行号指示器。在虚拟机的概念模型里，字节码解释器工作时就是通过改变这个计数器的值来选取下一条需要执行的字节码指令，分支、循环、跳转、异常处理、线程恢复等基础功能都需要依赖这个计数器来完成。 由于Java虚拟机的多线程是通过线程轮流切换并分配处理器执行时间的方式来实现的，在任何一个确定的时刻，一个处理器（对于多核心处理器来说是一个内核）都会执行一条线程中得指令。因此，为了线程切换后能恢复到正确的执行位置，每条线程都需要有一个独立的程序计数器，各条线程之间计数器互不影响，独立存储，我们称这类内存区域为“线程私有”的内存。 如果线程正在执行的是一个Java方法，这个计数器记录的是正在执行的虚拟机字节码指令的地址；如果正在执行的是Native方法，这个计数器值则为空（Undefined）。此内存区域是唯一一个在Java虚拟机规范中没有规定任何OutOfMemoryError情况的区域。 Java虚拟机栈与程序计数器一样，Java虚拟机栈（Java Virtual Machine Stacks）也是线程私有的，它的生命周期与线程相同。虚拟机栈描述的是Java方法执行的同时都会创建一个栈帧（Stack Frame）用于存储局部变量表、操作数栈、动态链接、方法出口等信息。每一个方法从调用直至执行完成的过程，就对应着一个栈帧在虚拟机栈中入栈到出栈的过程。 经常有人把Java内存区分为堆内存（Heap）和栈内存（Stack），这种分发比较粗糙，Java内存区域的划分实际上远比这复杂。这种划分方式的流行只能说明大多数程序员最关注的、与对象内存分配关系最密切的内存区域是这两块。而所指的“栈”就是现在讲的虚拟机栈，或者说是虚拟机栈中局部变量变部分。 局部变量表存放了编译器可知的各种基本数据类型（boolean、byte、char、short、int、float、long、double）对象引用（refrerence类型，它不等同于对象本身，可能是一个指向对象起始地址的引用指针，也可能是指向一个代表对象的句柄或其他与此对象相关的位置）和returnAddress类型（指向了一条字节码指令的地址）。 其中64位长度的long和double类型的数据会占用2个局部变量空间（Slot），其余的数据类型只占用1个。局部变量表所需要的内存空间在编译期间完成分配，当进入一个方法时，这个方法需要在帧中分配多大的局部变量空间是完全确定的，在方法运行期间不会改变局部变量表的大小。 在Java虚拟机规范中，对这个区域规定了两种异常状况：如果线程请求的栈深度大于虚拟机所允许的深度，将抛出StackOverFlowError异常；如果虚拟机栈可以动态扩展（当前大部分的Java虚拟机都可以动态扩展，只不过Java虚拟机规范中也允许固定长度的虚拟机栈），如果扩展时无法申请到足够的内存，就会抛出OutOfMemoryError异常。 本地方法栈本地方法栈（Native Method Stack）与虚拟机栈所发挥的作用是非常相似的，它们之间的区别不过时虚拟机栈为虚拟机执行Java方法（也就是字节码）服务，而本地方法栈则为虚拟机使用到的Native方法服务。在虚拟机规范中对本地方法栈中方法使用的语言、使用方式与数据结构并没有强制规定，因此具体的虚拟机可以自由实现它。甚至有的虚拟机（譬如Sun HotSpot虚拟机）直接把本地方法栈和虚拟机栈合二为一。与虚拟机一样，本地方法栈区域也会抛出StackOverFlowError和OutOfMemoryError异常。 Java堆对于大多数应用来说，Java堆（Java Heap）是Java虚拟机所管理的内存中最大的一块。Java堆是被所有线程共享的一块内存区域，在虚拟机启动时创建。此内存区域的唯一目的就是存放对象实例，几乎所有对象实例都在这里分配内存。这一点在Java虚拟机规范中的描述是：所有的对象实例以及数组都要在堆内存上分配，但是随着JIT编译器的发展与逃逸分析技术逐渐成熟，栈上分配、标量替换优化技术将会导致一些微妙的变化发生，所有的对象都分配在堆上也渐渐变得不是那么绝对了。 Java堆是垃圾收集器管理的主要区域，因此很多时候也被称为“GC堆”，幸好国内没有翻译成“垃圾堆”，从内存回收的角度来看，由于现在收集器基本都采用分带收集算法，所以Java堆中还可以细分为：新生代和老年代；再细致一点的有Eden空间、From Survivor空间、To Survivor空间等。从内存分配的角度来看，线程共享的Java堆中可能划分出多个线程私有的分配缓冲区（Thread Local Allocation Buffer，TLAB）。不过无论如何划分，都与存放内容无关，无论哪个区域，存储的都仍然是对象实例，进一步划分的目的是为了更好地回收内存，或者更快的分配内存。 根据Java虚拟机规范的规定，Java堆可以处于物理上不连续的内存空间中，只要逻辑上是连续的即可，就像我们的磁盘空间一样。在实现时，既可以实现成固定大小的，也可以是可扩展的，不过当前主流的虚拟机都是按照可扩展来实现的（通过-Xmx和-Xms控制）。如果在堆中没有内存完成实例分配，并且堆也无法再扩展时，将会抛出OutOfMemoryError异常。 方法区方法区（Method Area）与Java堆一样，是各个线程共享的内存区域，它用于存储已被虚拟机加载的类信息、常量、静态变量、即时编译器编译后的代码等数据。虽然Java虚拟机规范把方法区描述为堆的一个逻辑部分，但是它却有一个别名叫做Non-Heap（非堆），目的应该是与Java堆区分开来。 对于习惯在HotSpot虚拟机上开发、部署程序的开发者来说，很多人都更愿意把方法区成为“永久代”（Permanent Generation），本质上两者并不等价，仅仅是因为HotSpot虚拟机的设计团队选择把GC分代收集扩展至方法区而已，这样HotSpot的垃圾收集器可以像管理Java堆一样管理这部分内存，能够省去专门为方法区编写内存管理代码的工作。对于其他虚拟机（如BEA JRockit、IBM J9等）来说是不存在永久代的概念的。原则上，如何实现方法区属于虚拟机实现细节，不受虚拟机规范约束，但使用永久代来实现方法区，现在看来并不是一个好主意，因为这样更容易遇到内存溢出问题（永久代有XX:MaxPermSize的上限，J9和JRockit只要没有触碰到进程可用内存的上限，例如32位系统中的4GB，就不会出现问题），而且有极少数方法（例如String.intern())会因这个原因导致不同虚拟机下有不同的表现。因此，对于HotSpot虚拟机，根据官方发布的路线图信息，现在也有放弃永久代并逐步改为采用Native Memory来实现方法区的规划了，在目前已经发布的JDK1.7的HotSpot中，已经把原本房在永久代的字符串常量池移出。 Java虚拟机规范对方法区的限制非常宽松，除了和Java堆一样不需要连续的内存和可以选择固定大小或者可扩展外，还可以选择不实现垃圾收集。相对而言，垃圾收集行为在这个区域是比较少出现的，但并非数据进入了方法区就如永久代的名字一样“永久”存在了。这区域的内存回收目标主要是针对常量池的回收和对类型的卸载，一般来说，这个区域的回收“成绩”比较难以令人满意，尤其是类型的卸载，条件相当苛刻，但是这部分区域的回收确实是必要的。在Sun公司的BUG列表中，曾出现过的若干个严重的BUG就是由于低版本的HotSpot虚拟机对此区域未完全回收而导致内存泄漏。 根据Java虚拟机规范的规定，当方法区无法满足内存分配需求时，将抛出OutOfMemoryError异常。 参考资料：《深入理解Java虚拟机》]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>JVM</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[手把手带你写一个图片加载框架XDisplay]]></title>
    <url>%2Fproject_xdisplay%2F</url>
    <content type="text"><![CDATA[介绍开发中，加载图片是我们最常见的需求。为了开发方便和高效，一般我们都会选用成熟和高效的第三方加载框架，比如Glide，imageloader，fresco等等。但我们不能仅仅满足于此，在会用的同时我们要明白其加载流程以及其原理。最好能动手写一个简单的加载框架，算是加深了对图片加载流程和原理的进一步认识。 准备工作首先，我们需要知道一个图片加载框架从传入图片的地址到图片呈现出来，中间都经历了什么。我们可以简单将其归结为一下步骤： 1.检索应用内存缓存中是否已存在该图片，如果内存中存在，呈现出图片。本次加载完毕。2.如果内存中不存在，则先呈现出配置中的占位图，检索本地磁盘缓存是否存在该图片，如果存在，呈现出图片，更新内存缓存，本次加载完毕。3.如果本地磁盘缓存中不存在，则使用HTTP从网络下载该图片，然后呈现，然后更新内存缓存，更新本地磁盘缓存，本次加载完毕。 简单概括为下图 技术分析缓存部分这部分大家应该都知道我们用的是LRUCache，关于LRUCache的细节此处不做过多介绍，想深入了解的可以自行查找相关资料。还有本地缓存采用DiskLruCache，这个也是大家熟知的。 图片的下载HTTP下载部分采用HttpURLConnection来处理。 多任务并发的调度（重点部分）如果只处理一张图片的话，很简单，照着这个流程就能完成任务。但是，实际的应用场景中，很多都是需要同时加载大量的图片，也就是说同一时刻有N多个图片加载请求，这时，我们就需要对任务进行一个合适的调度分配来保证合理的加载。比如一个图片列表，打开页面后，快速的向下滑动，如果我们按照图片的请求顺序呈现，那么最后一张图片估计要等到花儿谢了才能呈现出来。。。所以我们需要对并发做一个智能的调度。 对于并发的请求，采用以下方案进行解决：首先我们需要定义一个线程池，比如说定义一个有5个核心线程的线程池。加载请求按照顺序依次加入任务队列，当线程池有空闲线程时，按先后顺序取出任务并顺序执行。当线程池没有空闲线程时，此时的并发数比较大，线程池处于等待状态，从队列尾部取出任务并提交线程池等待执行。 代码部分LruCacheManager 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970717273/** * Created by Taurus on 2017/6/2. */public class LruCacheManager &#123; private LruCache&lt;String,Bitmap&gt; mLruCache; public LruCacheManager(SettingBuilder settingBuilder)&#123; //LruCache的配置 mLruCache = new LruCache&lt;String, Bitmap&gt;(settingBuilder.getMaxMemoryCacheSize())&#123; @Override protected int sizeOf(String key, Bitmap value)&#123; return value.getRowBytes() * value.getHeight(); &#125; &#125;; &#125; /** * 加入或更新缓存 * @param key * @param bitmap * @return */ public Bitmap putCache(String key,Bitmap bitmap)&#123; Bitmap bitmapValue = getCache(key); if(bitmapValue==null)&#123; if(mLruCache!=null&amp;&amp;bitmap!=null) bitmapValue= mLruCache.put(key, bitmap); &#125; return bitmapValue; &#125; /** * 获取缓存 * @param key * @return */ public Bitmap getCache(String key)&#123; if(mLruCache!=null)&#123; return mLruCache.get(key); &#125; return null; &#125; /** * 清空缓存 */ public void deleteCache()&#123; if(mLruCache!=null) mLruCache.evictAll(); &#125; /** * 移除指定的缓存 * @param key */ public void removeCache(String key)&#123; if(mLruCache!=null) mLruCache.remove(key); &#125; /** * 当前缓存大小 * @return */ public int size()&#123; int size=0; if(mLruCache!=null) size+=mLruCache.size(); return size; &#125;&#125; DiskCacheManager ** * Created by Taurus on 2017/6/2. */public class DiskCacheManager &#123; private DiskLruCache mDiskLruCache; private SettingBuilder settingBuilder; public DiskCacheManager(SettingBuilder settingBuilder)&#123; this(settingBuilder.getCacheDir(),Util.getAppVersion(settingBuilder.getContext()),settingBuilder.getDiskCacheValueCount(),settingBuilder.getMaxDiskCacheSize()); this.settingBuilder = settingBuilder; &#125; private DiskCacheManager(File cacheDirectory, int appVersion,int valueCount, long maxSize)&#123; try &#123; if(!cacheDirectory.exists())&#123; cacheDirectory.mkdirs(); &#125; //DiskLruCache的配置 mDiskLruCache = DiskLruCache.open(cacheDirectory, appVersion, valueCount, maxSize); &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; public File getCacheDirectory()&#123; return settingBuilder.getCacheDir(); &#125; private String getCacheKey(String key)&#123; return settingBuilder.getCacheKeyProvider().getCacheKey(key); &#125; /** * 加入或更新缓存 * @param key * @param bitmap * @return */ public boolean putDiskCache(String key, Bitmap bitmap)&#123; DiskLruCache.Editor editor = null; try &#123; String md5Key = getCacheKey(key); if(mDiskLruCache.get(md5Key)!=null)&#123; return true; &#125; editor = mDiskLruCache.edit(md5Key); if(editor!=null)&#123; OutputStream outputStream= editor.newOutputStream(0); Util.Bitmap2OutputStream(bitmap,outputStream); if(outputStream!=null)&#123; editor.commit(); return true; &#125; else &#123; editor.abort(); return false; &#125; &#125; &#125;catch (Exception e)&#123; e.printStackTrace(); if(editor!=null)&#123; try &#123; editor.abort(); &#125; catch (IOException e1) &#123; e1.printStackTrace(); &#125; &#125; &#125; return false; &#125; /** * 刷新缓存 */ public void flushCache() &#123; if (mDiskLruCache != null) &#123; try &#123; mDiskLruCache.flush(); &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; &#125; /** * 获取缓存 * @param key * @return */ public Bitmap getDiskCache(String key)&#123; String md5Key = getCacheKey(key); Bitmap bitmap=null; try &#123; if(mDiskLruCache!=null)&#123; DiskLruCache.Snapshot snapshot = mDiskLruCache.get(md5Key); if(snapshot!=null)&#123; bitmap= BitmapFactory.decodeStream(snapshot.getInputStream(0)) ; &#125; &#125; &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; return bitmap; &#125; /** * 清空缓存 */ public void deleteDiskCache()&#123; try &#123; if(mDiskLruCache!=null)&#123; mDiskLruCache.delete(); &#125; &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; /** * 移除指定的缓存 * @param key */ public void removeDiskCache(String key)&#123; if(mDiskLruCache!=null)&#123; try &#123; mDiskLruCache.remove(key); &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; &#125; /** * 获取当前缓存的大小 * @return */ public int size()&#123; int size=0; if(mDiskLruCache!=null)&#123; size=(int) mDiskLruCache.size(); &#125; return size; &#125; /** * 关闭缓存 */ public void close()&#123; if(mDiskLruCache!=null)&#123; try &#123; mDiskLruCache.close(); &#125; catch (IOException e) &#123; e.printStackTrace(); &#125; &#125; &#125;&#125; 接下来我们看下重点部分，任务调度的代码，使用了Semaphore信号量计数。Semaphore semp = new Semaphore(5);我们可以把Semaphore看作是许可证，也就是说我们总共提供了5个许可证，获取一个许可证semp.acquire()就可以执行加载任务了。当无许可证时阻塞等待，直到有许可证释放semp.release()。 使用Semaphore，并结合Handler进行处理。代码如下： 123456789101112131415161718192021222324252627282930313233343536public void init(SettingBuilder settingBuilder)&#123; mExecutor = Executors.newFixedThreadPool(settingBuilder.getCoreThreadCount()); mThreadSemaphore = new Semaphore(settingBuilder.getCoreThreadCount()); new Thread()&#123; @Override public void run() &#123; super.run(); Looper.prepare(); mThreadHandler = new Handler()&#123; @Override public void handleMessage(Message msg) &#123; super.handleMessage(msg); try &#123; //获取许可 mThreadSemaphore.acquire(); Log.d(TAG,"ThreadSemaphore acquire..."); &#125; catch (InterruptedException e) &#123; e.printStackTrace(); &#125; final DisplayTask task = mQueue.pollLast(); final OnViewTarget viewTarget = task.getOnViewTarget(); if(task!=null)&#123; //execute task 代码略长，此处省略 &#125;else&#123; //释放许可 releaseSemaphore(); &#125; &#125; &#125;; if(onThreadHandlerListener!=null)&#123; onThreadHandlerListener.onInitFinish(); &#125; Looper.loop(); &#125; &#125;.start();&#125; 下载图片 123456789101112131415161718192021222324252627282930313233343536373839404142public static Bitmap downloadBitmap(String imageUrl, OnDownloadListener onDownloadListener) &#123; Bitmap bitmap = null; HttpURLConnection con = null; File temp = new File(CacheManager.getInstance().getDiskCacheDirectory(),UUID.randomUUID().toString()); try &#123; FileOutputStream outputStream = new FileOutputStream(temp); URL url = new URL(imageUrl); if(onDownloadListener!=null)&#123; onDownloadListener.onStart(); &#125; con = (HttpURLConnection) url.openConnection(); con.setConnectTimeout(5 * 1000); con.setReadTimeout(10 * 1000); int contentLength = con.getContentLength(); Log.d(TAG,"contentLength = " + contentLength); InputStream in = con.getInputStream(); byte[] buf = new byte[4*1024]; int len; int progress = 0; while ((len = in.read(buf))!=-1)&#123; outputStream.write(buf,0,len); outputStream.flush(); progress += len; if(onDownloadListener!=null)&#123; onDownloadListener.onProgress(progress,contentLength); &#125; &#125; if(onDownloadListener!=null)&#123; onDownloadListener.onFinish(); &#125; FileInputStream tempIn = new FileInputStream(temp); bitmap = BitmapFactory.decodeFileDescriptor(tempIn.getFD()); temp.delete(); &#125; catch (Exception e) &#123; e.printStackTrace(); &#125; finally &#123; if (con != null) &#123; con.disconnect(); &#125; &#125; return bitmap;&#125; 详细的处理细节见代码：https://github.com/jiajunhui/XDisplay 结语本篇只是就图片框架的原理和流程写了一个简单的图片加载框架，跟市面上的框架有很大差距。真正的图片加载框架需要考虑的东西非常多，就glide而言内部就包含了对GIF的支持，图片圆角的支持，动画的支持等等特点。所以我们还需继续进一步学习大牛的神作。]]></content>
      <categories>
        <category>我的开源项目</category>
      </categories>
      <tags>
        <tag>Image</tag>
        <tag>Load</tag>
        <tag>Display</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[从源码角度理解Android的Handler机制]]></title>
    <url>%2Fandroid_handler%2F</url>
    <content type="text"><![CDATA[简介说起Handler，我们一般用的最多的就是在子线程通过Handler发Message进行UI更新。当你发了一个Message后到底发生了什么就进入了Handler的handleMessage方法了？接下来我们从源码的角度对Handler一探究竟。 源码探秘当我们使用Handler时： 1234567private Handler mHandler = new Handler()&#123; @Override public void handleMessage(Message msg) &#123; super.handleMessage(msg); &#125;&#125;; 首先进行的是初始化Handler对象，那么我们就先看看Handler在进行初始化时都做了什么： 12345678910111213141516171819public Handler(Callback callback, boolean async) &#123; if (FIND_POTENTIAL_LEAKS) &#123; final Class&lt;? extends Handler&gt; klass = getClass(); if ((klass.isAnonymousClass() || klass.isMemberClass() || klass.isLocalClass()) &amp;&amp; (klass.getModifiers() &amp; Modifier.STATIC) == 0) &#123; Log.w(TAG, "The following Handler class should be static or leaks might occur: " + klass.getCanonicalName()); &#125; &#125; mLooper = Looper.myLooper(); if (mLooper == null) &#123; throw new RuntimeException( "Can't create handler inside thread that has not called Looper.prepare()"); &#125; mQueue = mLooper.mQueue; mCallback = callback; mAsynchronous = async;&#125; 看代码第11行： 1mLooper = Looper.myLooper(); 此处对mLooper成员变量进行了赋值，突然冒出来个Looper，什么鬼呢？我们顺藤摸瓜，进入Looper内看看。 1234567/** * Return the Looper object associated with the current thread. Returns * null if the calling thread is not associated with a Looper. */public static @Nullable Looper myLooper() &#123; return sThreadLocal.get();&#125; 123456private static void prepare(boolean quitAllowed) &#123; if (sThreadLocal.get() != null) &#123; throw new RuntimeException("Only one Looper may be created per thread"); &#125; sThreadLocal.set(new Looper(quitAllowed));&#125; 通过源码我们发现sThreadLocal是在prepare(boolean quitAllowed)中进行设置值操作的。那就有点郁闷了，一般我们使用Handler时，我们没有调用这个方法啊。其实啊，是系统帮我们调用过了。看下面这段代码： 123456789101112131415/** * Initialize the current thread as a looper, marking it as an * application's main looper. The main looper for your application * is created by the Android environment, so you should never need * to call this function yourself. See also: &#123;@link #prepare()&#125; */public static void prepareMainLooper() &#123; prepare(false); synchronized (Looper.class) &#123; if (sMainLooper != null) &#123; throw new IllegalStateException("The main Looper has already been prepared."); &#125; sMainLooper = myLooper(); &#125;&#125; 看第8行，调用了prepare(false),然后我们再看看代码的注释，大致意思就是这个方法是在application初始化时系统调用的，用户不需要自己去调用这个方法。 然后我们继续看Handler的初始化，看Handler代码块的第15行： 1mQueue = mLooper.mQueue; 通过源码我们了解到mQueue是MessageQueue类型的，又引入了一个新角色，其实这货就是我们口口相传的消息队列(其实内部是维护了一个Message的链表)。此处是由mLooper中的变量赋值的，进入Looper查看： 1234private Looper(boolean quitAllowed) &#123; mQueue = new MessageQueue(quitAllowed); mThread = Thread.currentThread();&#125; mQueue在Looper构造时进行了初始化。Handler的初始化完成。接下来我们继续沿着Handler的使用进行讲解。 我们使用Handler发消息时调用mHandler.sendMessage(msg),通过层层调用，最终进入了下面的这个方法： 12345678910111213141516171819202122232425262728293031323334353637/** * Enqueue a message into the message queue after all pending messages * before the absolute time (in milliseconds) &lt;var&gt;uptimeMillis&lt;/var&gt;. * &lt;b&gt;The time-base is &#123;@link android.os.SystemClock#uptimeMillis&#125;.&lt;/b&gt; * Time spent in deep sleep will add an additional delay to execution. * You will receive it in &#123;@link #handleMessage&#125;, in the thread attached * to this handler. * * @param uptimeMillis The absolute time at which the message should be * delivered, using the * &#123;@link android.os.SystemClock#uptimeMillis&#125; time-base. * * @return Returns true if the message was successfully placed in to the * message queue. Returns false on failure, usually because the * looper processing the message queue is exiting. Note that a * result of true does not mean the message will be processed -- if * the looper is quit before the delivery time of the message * occurs then the message will be dropped. */public boolean sendMessageAtTime(Message msg, long uptimeMillis) &#123; MessageQueue queue = mQueue; if (queue == null) &#123; RuntimeException e = new RuntimeException( this + " sendMessageAtTime() called with no mQueue"); Log.w("Looper", e.getMessage(), e); return false; &#125; return enqueueMessage(queue, msg, uptimeMillis);&#125;private boolean enqueueMessage(MessageQueue queue, Message msg, long uptimeMillis) &#123; msg.target = this; if (mAsynchronous) &#123; msg.setAsynchronous(true); &#125; return queue.enqueueMessage(msg, uptimeMillis);&#125; 看第32行的msg.target = this;既然是在Handler中的this赋值给了msg.target，也就是说target的Handler类型的，别急，下面讲消息的轮询时会用到。最终进入到了MessageQueue的enqueueMessage方法 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950boolean enqueueMessage(Message msg, long when) &#123; if (msg.target == null) &#123; throw new IllegalArgumentException("Message must have a target."); &#125; if (msg.isInUse()) &#123; throw new IllegalStateException(msg + " This message is already in use."); &#125; synchronized (this) &#123; if (mQuitting) &#123; IllegalStateException e = new IllegalStateException( msg.target + " sending message to a Handler on a dead thread"); Log.w(TAG, e.getMessage(), e); msg.recycle(); return false; &#125; msg.markInUse(); msg.when = when; Message p = mMessages; boolean needWake; if (p == null || when == 0 || when &lt; p.when) &#123; // New head, wake up the event queue if blocked. msg.next = p; mMessages = msg; needWake = mBlocked; &#125; else &#123; // Inserted within the middle of the queue. Usually we don't have to wake // up the event queue unless there is a barrier at the head of the queue // and the message is the earliest asynchronous message in the queue. needWake = mBlocked &amp;&amp; p.target == null &amp;&amp; msg.isAsynchronous(); Message prev; for (;;) &#123; prev = p; p = p.next; if (p == null || when &lt; p.when) &#123; break; &#125; if (needWake &amp;&amp; p.isAsynchronous()) &#123; needWake = false; &#125; &#125; msg.next = p; // invariant: p == prev.next prev.next = msg; &#125; // We can assume mPtr != 0 because mQuitting is false. if (needWake) &#123; nativeWake(mPtr); &#125; &#125; return true;&#125; 又引入了一个重要的元素Message，也就是我们称之为消息的对象类，该类包含了what、obj、callback等属性，比如我们区分不同的消息时通常是通过msg.what来进行分别处理。通过msg.obj来取出消息中包含的数据。 看上面20-43行，是对msg对象加入了链表序列的操作。 上面讲了这么多，似乎还是没有讲到最终消息是如何回调到handleMessage方法执行的。接下来我们继续看上文中提到的Looper类,看Looper中如下方法： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253/** * Run the message queue in this thread. Be sure to call * &#123;@link #quit()&#125; to end the loop. */public static void loop() &#123; final Looper me = myLooper(); if (me == null) &#123; throw new RuntimeException("No Looper; Looper.prepare() wasn't called on this thread."); &#125; final MessageQueue queue = me.mQueue; // Make sure the identity of this thread is that of the local process, // and keep track of what that identity token actually is. Binder.clearCallingIdentity(); final long ident = Binder.clearCallingIdentity(); for (;;) &#123; Message msg = queue.next(); // might block if (msg == null) &#123; // No message indicates that the message queue is quitting. return; &#125; // This must be in a local variable, in case a UI event sets the logger final Printer logging = me.mLogging; if (logging != null) &#123; logging.println("&gt;&gt;&gt;&gt;&gt; Dispatching to " + msg.target + " " + msg.callback + ": " + msg.what); &#125; final long traceTag = me.mTraceTag; if (traceTag != 0 &amp;&amp; Trace.isTagEnabled(traceTag)) &#123; Trace.traceBegin(traceTag, msg.target.getTraceName(msg)); &#125; try &#123; msg.target.dispatchMessage(msg); &#125; finally &#123; if (traceTag != 0) &#123; Trace.traceEnd(traceTag); &#125; &#125; if (logging != null) &#123; logging.println("&lt;&lt;&lt;&lt;&lt; Finished to " + msg.target + " " + msg.callback); &#125; // Make sure that during the course of dispatching the // identity of the thread wasn't corrupted. final long newIdent = Binder.clearCallingIdentity(); if (ident != newIdent) &#123; Log.wtf(TAG, "Thread identity changed from 0x" + Long.toHexString(ident) + " to 0x" + Long.toHexString(newIdent) + " while dispatching to " + msg.target.getClass().getName() + " " + msg.callback + " what=" + msg.what); &#125; msg.recycleUnchecked(); &#125;&#125; 篇幅有点长，我们只看关键部分，看第15行，一个无限制的for循环，这就是传说中的不断轮训消息队列。通过queue.next();不断的取消息。 看第32行，msg.target.dispatchMessage(msg);还记得上文中提到的msg.target吗？target即是该消息所属的Handler。也就是说dispatchMessage()是在Handler类中执行的。 123456789101112131415/** * Handle system messages here. */public void dispatchMessage(Message msg) &#123; if (msg.callback != null) &#123; handleCallback(msg); &#125; else &#123; if (mCallback != null) &#123; if (mCallback.handleMessage(msg)) &#123; return; &#125; &#125; handleMessage(msg); &#125;&#125; 找到了吧，最终调用了handleMessage方法。 等等……这个loop()方法谁调用的呢？什么时候开始执行轮询的呢？ 记得上文中提到的prepareMainLooper()是系统调用的吧，其实loop方法也是由系统调用prepareMainLooper()之后又顺手把loop给调用了。我也找到了这段代码的出处，在ActivityThread中。 ActivityThread其实就是我们经常说的UI thread，也就是主线程。应用启动时由系统创建。我们都知道主线程可以使用Handler进行异步通信，因为主线程中已经创建了Looper，而这个Looper就是在这里创建的。这些其实我们的上文分析即可得出。如果其他线程需要使用Handler通信，就要自己去创建Looper。 对于主线程的Looper的初始化我们看下ActivityThread的main函数： 123456789101112131415public final class ActivityThread &#123; //......some code public static void main(String[] args) &#123; //......some code // 在这儿调用 Looper.prepareMainLooper, 为应用的主线程创建Looper Looper.prepareMainLooper(); ActivityThread thread = new ActivityThread(); if (sMainThreadHandler == null) &#123; sMainThreadHandler = thread.getHandler(); &#125; Looper.loop(); //......some code &#125; //......some code&#125; 看到了吧，在执行完Looper.prepareMainLooper();后，ActivityThread帮我们调用了loop轮询。所以我们一般在使用Handler时直接初始化一个Handler，然后发消息，之后等着在handleMessage中等待回调就OK了，其实是系统帮我们做了一部分工作了。 对于ActivityThread此处不做过多讲解，有时间在后面的博文中给大家掰扯掰扯应用启动的那些事儿。 以上讲解的都是在应用主线程下使用Handler的，如果我们想在子线程中使用，咋办呢？不如我们直接按原来的写法试试： 123456789101112new Thread()&#123; @Override public void run() &#123; Handler handler = new Handler()&#123; @Override public void handleMessage(Message msg) &#123; super.handleMessage(msg); &#125; &#125;; &#125;&#125;.start(); 一运行，挂了。。。。抛了个异常 1java.lang.RuntimeException: Can't create handler inside thread that has not called Looper.prepare() 咋回事儿呢？看本文上面的那段Handler初始化代码的第11行就明白了。当检测到mLooper为空时就会抛出这段异常。而上文中我们也说到了，mLooper的赋值是在prepare方法中进行的，所以正确的使用方案就有了，就如同应用主线程ActivityThread一样，我们需要先prepare一下，只不过主线程调用的是prepareMainLooper()，我们要调用prepare()，同样我们也需要调用loop()方法开始消息的轮询。 所以在子线程中，正确的使用如下： 1234567891011121314new Thread()&#123; @Override public void run() &#123; Looper.prepare(); Handler handler = new Handler()&#123; @Override public void handleMessage(Message msg) &#123; super.handleMessage(msg); &#125; &#125;; Looper.loop(); &#125;&#125;.start(); 总结来张图（图片来自网络） 我们重新捋一捋：首先由这么几个重要的角色，Handler、Looper、MessageQueue、Message。在主线程下，应用启动时系统帮我们初始化了主线程的MainLooper，并开启了loop循环，我们在UI线程初始化完Handler时，可以直接使用sendMessage方法发各种Message消息，然后我们的Message进入了MessageQueue，loop不断轮询，当从MessageQueue中取到消息时就通过Message的target（即所属的Handler对象）回调到Handler的handleMessage方法，最终我们就可以快乐的在handleMessage方法里面根据不同的msg.what进行消息的区分进行不同的操作。 疑点解惑主线程的Looper中的loop()循环为什么不会卡死主线程？我们先看一段ActivityThread的代码： 123456789101112131415161718192021222324public static void main(String[] args) &#123; //... Looper.prepareMainLooper(); ActivityThread thread = new ActivityThread(); thread.attach(false); if (sMainThreadHandler == null) &#123; sMainThreadHandler = thread.getHandler(); &#125; if (false) &#123; Looper.myLooper().setMessageLogging(new LogPrinter(Log.DEBUG, "ActivityThread")); &#125; // End of event ActivityThreadMain. Trace.traceEnd(Trace.TRACE_TAG_ACTIVITY_MANAGER); Looper.loop(); throw new RuntimeException("Main thread loop unexpectedly exited");&#125; 看最后一行代码，抛出一个运行时异常“Main thread loop unexpectedly exited”(主线程异常退出)，也就是说如果loop循环执行完的话系统就判定为应用异常退出了。其实这里涉及到了线程/进程问题。以下转载自知乎：https://www.zhihu.com/question/34652589进程：每个app运行时前首先创建一个进程，该进程是由Zygote fork出来的，用于承载App上运行的各种Activity/Service等组件。进程对于上层应用来说是完全透明的，这也是google有意为之，让App程序都是运行在Android Runtime。大多数情况一个App就运行在一个进程中，除非在AndroidManifest.xml中配置Android:process属性，或通过native代码fork进程。 线程：线程对应用来说非常常见，比如每次new Thread().start都会创建一个新的线程。该线程与App所在进程之间资源共享，从Linux角度来说进程与线程除了是否共享资源外，并没有本质的区别，都是一个task_struct结构体，在CPU看来进程或线程无非就是一段可执行的代码，CPU采用CFS调度算法，保证每个task都尽可能公平的享有CPU时间片。有了这么准备，再说说死循环问题：对于线程既然是一段可执行的代码，当可执行代码执行完成后，线程生命周期便该终止了，线程退出。而对于主线程，我们是绝不希望会被运行一段时间，自己就退出，那么如何保证能一直存活呢？简单做法就是可执行代码是能一直执行下去的，死循环便能保证不会被退出，例如，binder线程也是采用死循环的方法，通过循环方式不同与Binder驱动进行读写操作，当然并非简单地死循环，无消息时会休眠。但这里可能又引发了另一个问题，既然是死循环又如何去处理其他事务呢？通过创建新线程的方式。真正会卡死主线程的操作是在回调方法onCreate/onStart/onResume等操作时间过长，会导致掉帧，甚至发生ANR，looper.loop本身不会导致应用卡死。 另外，ActivityThread实际上并非线程，不像HandlerThread类，ActivityThread并没有真正继承Thread类，只是往往运行在主线程，给人以线程的感觉，其实承载ActivityThread的主线程就是由Zygote fork而创建的进程。主线程的死循环一直运行是不是特别消耗CPU资源呢？ 其实不然，这里就涉及到Linux pipe/epoll机制，简单说就是在主线程的MessageQueue没有消息时，便阻塞在loop的queue.next()中的nativePollOnce()方法里，此时主线程会释放CPU资源进入休眠状态，直到下个消息到达或者有事务发生，通过往pipe管道写端写入数据来唤醒主线程工作。这里采用的epoll机制，是一种IO多路复用机制，可以同时监控多个描述符，当某个描述符就绪(读或写就绪)，则立刻通知相应程序进行读或写操作，本质同步I/O，即读写是阻塞的。 所以说，主线程大多数时候都是处于休眠状态，并不会消耗大量CPU资源。 Activity的生命周期是怎么实现在死循环体外能够执行起来的？ActivityThread的内部类H继承于Handler，通过handler消息机制，简单说Handler机制用于同一个进程的线程间通信。Activity的生命周期都是依靠主线程的Looper.loop，当收到不同Message时则采用相应措施：在H.handleMessage(msg)方法中，根据接收到不同的msg，执行相应的生命周期。 比如收到msg=H.LAUNCH_ACTIVITY，则调用ActivityThread.handleLaunchActivity()方法，最终会通过反射机制，创建Activity实例，然后再执行Activity.onCreate()等方法； 再比如收到msg=H.PAUSE_ACTIVITY，则调用ActivityThread.handlePauseActivity()方法，最终会执行Activity.onPause()等方法。 上述过程，我只挑核心逻辑讲，真正该过程远比这复杂。 归结如下：A. Android应用程序的消息处理机制由消息循环、消息发送和消息处理三个部分组成的。B. Android应用程序的主线程在进入消息循环过程前，会在内部创建一个Linux管道（Pipe），这个管道的作用是使得Android应用程序主线程在消息队列为空时可以进入空闲等待状态，并且使得当应用程序的消息队列有消息需要处理时唤醒应用程序的主线程。C. Android应用程序的主线程进入空闲等待状态的方式实际上就是在管道的读端等待管道中有新的内容可读，具体来说就是是通过Linux系统的Epoll机制中的epoll_wait函数进行的。D. 当往Android应用程序的消息队列中加入新的消息时，会同时往管道中的写端写入内容，通过这种方式就可以唤醒正在等待消息到来的应用程序主线程。E. 当应用程序主线程在进入空闲等待前，会认为当前线程处理空闲状态，于是就会调用那些已经注册了的IdleHandler接口，使得应用程序有机会在空闲的时候处理一些事情。 结论：阻塞是有的，但是不会卡住主要原因有2个1，epoll模型当没有消息的时候会epoll.wait，等待句柄写的时候再唤醒，这个时候其实是阻塞的。2，所有的ui操作都通过handler来发消息操作。比如屏幕刷新16ms一个消息，你的各种点击事件，所以就会有句柄写操作，唤醒上文的wait操作，所以不会被卡死了。 所以当loop真的执行完了说明应用异常退出了。]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Handler</tag>
        <tag>Message</tag>
        <tag>Looper</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android开发-好书推荐]]></title>
    <url>%2Fother_book_recommend%2F</url>
    <content type="text"><![CDATA[学习，是个永无止境的操作。书籍，在提升能力上有很大作用，也许某句话、某行代码就能让你醍醐灌顶。从我自己学习过程中看过的几本书推荐给大家。 以下书刊在淘宝、亚马逊等各大电商网站上均能买到，希望对大家有用。 Android入门级郭大神的《第一行代码》 Android提高篇《Android软件安全与逆向分析》，这本书东西较多，毕竟是逆向破解方面的，注重实践。目前我也没看太多，还在深挖。 Java提高篇《深入理解Java虚拟机》，这本书主要讲解Java虚拟机的发展历程以及Java虚拟机的设计理念和内存模型以及优化策略等。]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>书籍推荐</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[文件操作工具库FileBase]]></title>
    <url>%2Fproject_file_base%2F</url>
    <content type="text"><![CDATA[介绍文件的操作在项目中是必然用到的，为了方便这些操作，封装了个库FileBase.包含了目录管理基类、对文件的常用操作、对Assets的操作、存储器信息的获取以及一些其他的操作。 依赖： 123dependencies &#123; compile 'com.kk.taurus.filebase:FileBase:1.1.4'&#125; 主要功能目录管理助手我们开发应用时一般都会有这样的需求场景：在存储器里面为自己的APP创建一个存储目录，用于存放下载的文件、本地配置信息、缓存的文件、临时文件等等。我们习惯的做法是：在存储器下判断是否存在我们的文件夹A，如果没有，创建一个文件夹A，然后在文件夹A里面再分别创建cache、download、temp等目录。如果再有别的主目录需要管理的话，看上去就相当混乱。那么能不能简化这些操作，而且让代码看上去条理清晰呢。我的方案来了，封装了一个基类，使用时只需要继承并提供一个主目录名称，需要获取某个子目录的路径时，直接提供文件夹的名字即可，无需关心文件夹在不在，需不需要创建等问题。 示例场景：应用需要在存储器下建立一个主目录TestFileBase，且有一下子目录：image、image/cache、temp、assets、log。 使用代码： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859package com.taurus.directorydemo;import android.content.Context;import com.kk.taurus.filebase.base.FileBase;import java.io.File;/** * Created by Taurus on 2016/12/23. */public class TestFileBase extends FileBase &#123; public static final String APP_MANAGE_ROOT_DIR_NAME = "TestFileBase"; public static final String APP_MANAGE_IMAGE_DIR_NAME = "image"; public static final String APP_MANAGE_IMAGE_CACHE_DIR_NAME = "image/cache"; public static final String APP_MANAGE_TEMP_DIR_NAME = "temp"; public static final String APP_MANAGE_ASSETS_COPY_DIR_NAME = "assets"; public static final String APP_MANAGE_LOG_DIR_NAME = "log"; public TestFileBase(Context context) &#123; super(context); &#125; public File getImageDir()&#123; return createDir(APP_MANAGE_IMAGE_DIR_NAME); &#125; public File getImageCacheDir()&#123; return createDir(APP_MANAGE_IMAGE_CACHE_DIR_NAME); &#125; public File getTempDir()&#123; return createDir(APP_MANAGE_TEMP_DIR_NAME); &#125; public File getAssetsDir()&#123; return createDir(APP_MANAGE_ASSETS_COPY_DIR_NAME); &#125; public File getLogDir()&#123; return createDir(APP_MANAGE_LOG_DIR_NAME); &#125; @Override public String getManageRootDirName() &#123; return APP_MANAGE_ROOT_DIR_NAME; &#125; @Override public int getRootParentDirType() &#123; return MANAGE_PARENT_DIR_APP_EXTERNAL_ROOT; &#125; @Override public int getRootParentSpareDirType() &#123; return MANAGE_PARENT_DIR_APP_EXTERNAL_CACHE_FILES; &#125;&#125; 首先我们需要继承FileBase，然后我们只需要定义主目录名字以及所有子目录的名字即可。获取时直接依靠基类的createDir()去return即可。 getRootParentDirType()这个方法是需要用户提供一个你主目录的所属位置类型，如果你想放置在外置存储器下，就需要返回MANAGE_PARENT_DIR_APP_EXTERNAL_ROOT。 总共提供了四种位置类型：应用私有的files目录、应用私有的cache目录、应用的外置cache目录、以及外置存储器主目录。分别对应于以下值： 12345678910111213141516171819/** * manage parent dir type . context.getFilesDir() */int MANAGE_PARENT_DIR_APP_FILES = 1 &lt;&lt; 1;/** * manage parent dir type . context.getCacheDir() */int MANAGE_PARENT_DIR_APP_CACHE_FILES = 1 &lt;&lt; 2;/** * manage parent dir type . context.getExternalCacheDir() */int MANAGE_PARENT_DIR_APP_EXTERNAL_CACHE_FILES = 1 &lt;&lt; 3;/** * manage parent dir type . Environment.getExternalStorageDirectory() */int MANAGE_PARENT_DIR_APP_EXTERNAL_ROOT = 1 &lt;&lt; 4; getRootParentSpareDirType()这个方法是需要用户提供一个备用目录类型。如果在你提供的目录所属位置下创建失败时会尝试在备用目录下进行相关操作。 Assets相关操作1public static InputStream getAssets(Context context, String fileName) 获取指定文件名的输入流。 1public static String[] listAssets(Context context, String dirName) 在assets目录下，获取指定文件夹下的文件列表。 1public static String getAssetsStringContent(Context context, String fileName) 获取指定文件名的文本内容。 1public static InputStream getAssets(Context context,String[] dirLevel, String fileName) 获取多级目录下的文件输入流。如路径为assets/dir1/dir2/test.txt，获取时dirLevel参数为 new String[]{“dir1”,”dir2”}. 1public static boolean copyAssetsToDir(Context context, String fileName, File toDir) 拷贝assets下的文件到指定目录下。 1public static boolean unZipAssets(Context context,String fileName,File destinationDir, boolean rewrite) 解压assets下的压缩包到指定目录下。 FileEngine工具主要包含文件的筛选、排序、删除、拷贝等操作，详见代码，这里不做过多讲解。 StorageEngine主要包含获取系统的存储，包含外置存储或外接存储（U盘等），以及获取其相关信息（可用空间和总空间等）。]]></content>
      <categories>
        <category>我的开源项目</category>
      </categories>
      <tags>
        <tag>文件管理</tag>
        <tag>File</tag>
        <tag>Assets</tag>
        <tag>Storage</tag>
        <tag>Copy</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android组件之内容提供者ContentProvider]]></title>
    <url>%2Fandroid_content_provider%2F</url>
    <content type="text"><![CDATA[简述内容提供者管理对中央数据存储区的访问。内容提供程序是 Android 应用的一部分，通常提供自己的 UI 来使用数据。 但是，内容提供程序主要旨在供其他应用使用，这些应用使用提供程序客户端对象来访问提供程序。 提供程序与提供程序客户端共同提供一致的标准数据界面，该界面还可处理跨进程通信并保护数据访问的安全性。 本文介绍了以下基础知识： 内容提供程序的工作方式。 用于从内容提供程序检索数据的 API。 用于在内容提供程序中插入、更新或删除数据的 API。 其他有助于使用提供程序的 API 功能。 以下内容来自Google文档。 概览内容提供程序以一个或多个表（与在关系型数据库中找到的表类似）的形式将数据呈现给外部应用。 行表示提供程序收集的某种数据类型的实例，行中的每个列表示为实例收集的每条数据。 例如，Android 平台的内置提供程序之一是用户字典，它会存储用户想要保存的非标准字词的拼写。 表 1 描述了数据在此提供程序表中的显示情况： 表 1. 用户字典示例表格。在表 1 中，每行表示可能无法在标准词典中找到的字词实例。 每列表示该字词的某些数据，如该字词首次出现时的语言区域。 列标题是存储在提供程序中的列名称。 要引用行的语言区域，需要引用其 locale 列。对于此提供程序，_ID 列充当由提供程序自动维护的“主键”列。 注：提供程序无需具有主键，也无需将 _ID 用作其主键的列名称（如果存在主键）。 但是，如果您要将来自提供程序的数据与 ListView 绑定，则其中一个列名称必须是 _ID。 显示查询结果部分详细说明了此要求。 访问提供程序应用从具有 ContentResolver 客户端对象的内容提供程序访问数据。 此对象具有调用提供程序对象（ContentProvider 的某个具体子类的实例）中同名方法的方法。 ContentResolver 方法可提供持续存储的基本“CRUD”（创建、检索、更新和删除）功能。 客户端应用进程中的 ContentResolver 对象和拥有提供程序的应用中的 ContentProvider 对象可自动处理跨进程通信。 ContentProvider 还可充当其数据存储区和表格形式的数据外部显示之间的抽象层。 注：要访问提供程序，您的应用通常需要在其清单文件中请求特定权限。 内容提供程序权限部分详细介绍了此内容。 例如，要从用户字典提供程序中获取字词及其语言区域的列表，则需调用 ContentResolver.query()。 query() 方法会调用用户字典提供程序所定义的 ContentProvider.query() 方法。 以下代码行显示了 ContentResolver.query() 调用： 1234567// Queries the user dictionary and returns resultsmCursor = getContentResolver().query( UserDictionary.Words.CONTENT_URI, // The content URI of the words table mProjection, // The columns to return for each row mSelectionClause // Selection criteria mSelectionArgs, // Selection criteria mSortOrder); // The sort order for the returned rows 表 2 显示了 query(Uri,projection,selection,selectionArgs,sortOrder) 的参数如何匹配 SQL SELECT 语句： 表 2. Query() 与 SQL 查询对比。 内容 URI内容 URI 是用于在提供程序中标识数据的 URI。内容 URI 包括整个提供程序的符号名称（其授权）和一个指向表的名称（路径）。 当您调用客户端方法来访问提供程序中的表时，该表的内容 URI 将是其参数之一。 在前面的代码行中，常量 CONTENT_URI 包含用户字典的“字词”表的内容 URI。 ContentResolver 对象会分析出 URI 的授权，并通过将该授权与已知提供程序的系统表进行比较，来“解析”提供程序。 然后， ContentResolver 可以将查询参数分派给正确的提供程序。 ContentProvider 使用内容 URI 的路径部分来选择要访问的表。 提供程序通常会为其公开的每个表显示一条路径。 在前面的代码行中，“字词”表的完整 URI 是： 1content://user_dictionary/words 其中，user_dictionary 字符串是提供程序的授权，words 字符串是表的路径。 字符串 content://（架构）始终显示，并将此标识为内容 URI。 许多提供程序都允许您通过将 ID 值追加到 URI 末尾来访问表中的单个行。 例如，要从用户字典中检索 _ID 为 4 的行，则可使用此内容 URI： 1Uri singleUri = ContentUris.withAppendedId(UserDictionary.Words.CONTENT_URI,4); 在检索到一组行后想要更新或删除其中某一行时通常会用到 ID 值。 注：Uri 和 Uri.Builder 类包含根据字符串构建格式规范的 URI 对象的便利方法。 ContentUris 包含一些可以将 ID 值轻松追加到 URI 后的方法。 前面的代码段就是使用 withAppendedId() 将 ID 追加到 UserDictionary 内容 URI 后。 从提供程序检索数据本节将以用户字典提供程序为例，介绍如何从提供程序中检索数据。 为了明确进行说明，本节中的代码段将在“UI 线程”上调用 ContentResolver.query()。但在实际代码中，您应该在单独线程上异步执行查询。 执行此操作的方式之一是使用 CursorLoader 类，加载器指南中对此有更为详细的介绍。 此外，前述代码行只是片段；它们不会显示整个应用。 要从提供程序中检索数据，请按照以下基本步骤执行操作： 请求对提供程序的读取访问权限。定义将查询发送至提供程序的代码。 请求读取访问权限要从提供程序检索数据，您的应需要具备对提供程序的“读取访问”权限。 您无法在运行时请求此权限；相反，您需要使用元素和提供程序定义的准确权限名称，在清单文件中指明您需要此权限。 在您的清单文件中指定此元素后，您将有效地为应用“请求”此权限。 用户安装您的应用时，会隐式授予允许此请求。 要找出您正在使用的提供程序的读取访问权限的准确名称，以及提供程序使用的其他访问权限的名称，请查看提供程序的文档。 内容提供程序权限部分详细介绍了权限在访问提供程序过程中的作用。 用户字典提供程序在其清单文件中定义了权限 android.permission.READ_USER_DICTIONARY，因此希望从提供程序中进行读取的应用必需请求此权限。 构建查询从提供程序中检索数据的下一步是构建查询。第一个代码段定义某些用于访问用户字典提供程序的变量： 12345678910111213// A "projection" defines the columns that will be returned for each rowString[] mProjection =&#123; UserDictionary.Words._ID, // Contract class constant for the _ID column name UserDictionary.Words.WORD, // Contract class constant for the word column name UserDictionary.Words.LOCALE // Contract class constant for the locale column name&#125;;// Defines a string to contain the selection clauseString mSelectionClause = null;// Initializes an array to contain selection argumentsString[] mSelectionArgs = &#123;""&#125;; 下一个代码段以用户字典提供程序为例，显示了如何使用 ContentResolver.query()。 提供程序客户端查询与 SQL 查询类似，并且包含一组要返回的列、一组选择条件和排序顺序。 查询应该返回的列集被称为投影（变量 mProjection）。 用于指定要检索的行的表达式分割为选择子句和选择参数。 选择子句是逻辑和布尔表达式、列名称和值（变量 mSelectionClause）的组合。 如果您指定了可替换参数 ? 而非值，则查询方法会从选择参数数组（变量 mSelectionArgs）中检索值。 在下一个代码段中，如果用户未输入字词，则选择子句将设置为 null，而且查询会返回提供程序中的所有字词。 如果用户输入了字词，选择子句将设置为 UserDictionary.Words.WORD + “ = ?” 且选择参数数组的第一个元素将设置为用户输入的字词。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253/* * This defines a one-element String array to contain the selection argument. */String[] mSelectionArgs = &#123;""&#125;;// Gets a word from the UImSearchString = mSearchWord.getText().toString();// Remember to insert code here to check for invalid or malicious input.// If the word is the empty string, gets everythingif (TextUtils.isEmpty(mSearchString)) &#123; // Setting the selection clause to null will return all words mSelectionClause = null; mSelectionArgs[0] = "";&#125; else &#123; // Constructs a selection clause that matches the word that the user entered. mSelectionClause = UserDictionary.Words.WORD + " = ?"; // Moves the user's input string to the selection arguments. mSelectionArgs[0] = mSearchString;&#125;// Does a query against the table and returns a Cursor objectmCursor = getContentResolver().query( UserDictionary.Words.CONTENT_URI, // The content URI of the words table mProjection, // The columns to return for each row mSelectionClause // Either null, or the word the user entered mSelectionArgs, // Either empty, or the string the user entered mSortOrder); // The sort order for the returned rows// Some providers return null if an error occurs, others throw an exceptionif (null == mCursor) &#123; /* * Insert code here to handle the error. Be sure not to use the cursor! You may want to * call android.util.Log.e() to log this error. * */// If the Cursor is empty, the provider found no matches&#125; else if (mCursor.getCount() &lt; 1) &#123; /* * Insert code here to notify the user that the search was unsuccessful. This isn't necessarily * an error. You may want to offer the user the option to insert a new row, or re-type the * search term. */&#125; else &#123; // Insert code here to do something with the results&#125; 此查询与 SQL 语句相似： 1SELECT _ID, word, locale FROM words WHERE word = &lt;userinput&gt; ORDER BY word ASC; 在此 SQL 语句中，会使用实际的列名称而非协定类常量。 防止恶意输入如果内容提供程序管理的数据位于 SQL 数据库中，将不受信任的外部数据包括在原始 SQL 语句中可能会导致 SQL 注入。 考虑此选择子句： 12// Constructs a selection clause by concatenating the user's input to the column nameString mSelectionClause = "var = " + mUserInput; 如果您执行此操作，则会允许用户将恶意 SQL 串连到 SQL 语句上。 例如，用户可以为 mUserInput 输入“nothing; DROP TABLE ;”，这会生成选择子句 var = nothing; DROP TABLE ;。 由于选择子句是作为 SQL 语句处理，因此这可能会导致提供程序擦除基础 SQLite 数据库中的所有表（除非提供程序设置为可捕获 SQL 注入尝试）。 要避免此问题，可使用一个用于将 ? 作为可替换参数的选择子句以及一个单独的选择参数数组。 执行此操作时，用户输入直接受查询约束，而不解释为 SQL 语句的一部分。 由于用户输入未作为 SQL 处理，因此无法注入恶意 SQL。请使用此选择子句，而不要使用串连来包括用户输入： 12// Constructs a selection clause with a replaceable parameterString mSelectionClause = "var = ?"; 按如下所示设置选择参数数组： 12// Defines an array to contain the selection argumentsString[] selectionArgs = &#123;""&#125;; 按如下所示将值置于选择参数数组中： 12// Sets the selection argument to the user's inputselectionArgs[0] = mUserInput; 一个用于将 ? 用作可替换参数的选择子句和一个选择参数数组是指定选择的首选方式，即使提供程序并未基于 SQL 数据库。 显示查询结果ContentResolver.query() 客户端方法始终会返回符合以下条件的 Cursor：包含查询的投影为匹配查询选择条件的行指定的列。 Cursor 对象为其包含的行和列提供随机读取访问权限。 通过使用 Cursor 方法，您可以循环访问结果中的行、确定每个列的数据类型、从列中获取数据，并检查结果的其他属性。 某些 Cursor 实现会在提供程序的数据发生更改时自动更新对象和/或在 Cursor 更改时触发观察程序对象中的方法。 注：提供程序可能会根据发出查询的对象的性质来限制对列的访问。 例如，联系人提供程序会限定只有同步适配器才能访问某些列，因此不会将它们返回至 Activity 或服务。 如果没有与选择条件匹配的行，则提供程序会返回 Cursor.getCount() 为 0（空游标）的 Cursor 对象。 如果出现内部错误，查询结果将取决于具体的提供程序。它可能会选择返回 null，或引发 Exception。 由于 Cursor 是行“列表”，因此显示 Cursor 内容的良好方式是通过 SimpleCursorAdapter 将其与 ListView 关联。 以下代码段将延续上一代码段的代码。它会创建一个包含由查询检索到的 Cursor 的 SimpleCursorAdapter 对象，并将此对象设置为 ListView 的适配器： 123456789101112131415161718192021// Defines a list of columns to retrieve from the Cursor and load into an output rowString[] mWordListColumns =&#123; UserDictionary.Words.WORD, // Contract class constant containing the word column name UserDictionary.Words.LOCALE // Contract class constant containing the locale column name&#125;;// Defines a list of View IDs that will receive the Cursor columns for each rowint[] mWordListItems = &#123; R.id.dictWord, R.id.locale&#125;;// Creates a new SimpleCursorAdaptermCursorAdapter = new SimpleCursorAdapter( getApplicationContext(), // The application's Context object R.layout.wordlistrow, // A layout in XML for one row in the ListView mCursor, // The result from the query mWordListColumns, // A string array of column names in the cursor mWordListItems, // An integer array of view IDs in the row layout 0); // Flags (usually none are needed)// Sets the adapter for the ListViewmWordList.setAdapter(mCursorAdapter); 注：要通过 Cursor 支持 ListView，游标必需包含名为 _ID 的列。 正因如此，前文显示的查询会为“字词”表检索 _ID 列，即使 ListView 未显示该列。 此限制也解释了为什么大多数提供程序的每个表都具有 _ID 列。 从查询结果中获取数据您可以将查询结果用于其他任务，而不是仅显示它们。例如，您可以从用户字典中检索拼写，然后在其他提供程序中查找它们。 要执行此操作，您需要在 Cursor 中循环访问行： 1234567891011121314151617181920212223242526272829// Determine the column index of the column named "word"int index = mCursor.getColumnIndex(UserDictionary.Words.WORD);/* * Only executes if the cursor is valid. The User Dictionary Provider returns null if * an internal error occurs. Other providers may throw an Exception instead of returning null. */if (mCursor != null) &#123; /* * Moves to the next row in the cursor. Before the first movement in the cursor, the * "row pointer" is -1, and if you try to retrieve data at that position you will get an * exception. */ while (mCursor.moveToNext()) &#123; // Gets the value from the column. newWord = mCursor.getString(index); // Insert code here to process the retrieved word. ... // end of while loop &#125;&#125; else &#123; // Insert code here to report an error if the cursor is null or the provider threw an exception.&#125; Cursor 实现包含多个用于从对象中检索不同类型的数据的“获取”方法。 例如，上一个代码段使用 getString()。 它们还具有 getType() 方法，该方法会返回指示列的数据类型的值。 内容提供程序权限提供程序的应用可以指定其他应用访问提供程序的数据所必需的权限。 这些权限可确保用户了解应用将尝试访问的数据。 根据提供程序的要求，其他应用会请求它们访问提供程序所需的权限。 最终用户会在安装应用时看到所请求的权限。 如果提供程序的应用未指定任何权限，则其他应用将无权访问提供程序的数据。 但是，无论指定权限为何，提供程序的应用中的组件始终具有完整的读取和写入访问权限。 如前所述，用户字典提供程序需要 android.permission.READ_USER_DICTIONARY 权限才能从中检索数据。 提供程序具有用于插入、更新或删除数据的单独 android.permission.WRITE_USER_DICTIONARY 权限。 要获取访问提供程序所需的权限，应用将通过其清单文件中的 元素来请求这些权限。Android 软件包管理器安装应用时，用户必须批准该应用请求的所有权限。 如果用户批准所有权限，软件包管理器将继续安装；如果用户未批准这些权限，软件包管理器将中止安装。 以下 元素会请求对用户字典提供程序的读取访问权限： 1&lt;uses-permission android:name="android.permission.READ_USER_DICTIONARY"&gt; 安全与权限指南中详细介绍了权限对提供程序访问的影响。 插入、更新和删除数据与从提供程序检索数据的方式相同，也可以通过提供程序客户端和提供程序 ContentProvider 之间的交互来修改数据。 您通过传递到 ContentProvider 的对应方法的参数来调用 ContentResolver 方法。 提供程序和提供程序客户端会自动处理安全性和跨进程通信。 插入数据要将数据插入提供程序，可调用 ContentResolver.insert() 方法。此方法会在提供程序中插入新行并为该行返回内容 URI。 此代码段显示如何将新字词插入用户字典提供程序： 123456789101112131415161718192021// Defines a new Uri object that receives the result of the insertionUri mNewUri;...// Defines an object to contain the new values to insertContentValues mNewValues = new ContentValues();/* * Sets the values of each column and inserts the word. The arguments to the "put" * method are "column name" and "value" */mNewValues.put(UserDictionary.Words.APP_ID, "example.user");mNewValues.put(UserDictionary.Words.LOCALE, "en_US");mNewValues.put(UserDictionary.Words.WORD, "insert");mNewValues.put(UserDictionary.Words.FREQUENCY, "100");mNewUri = getContentResolver().insert( UserDictionary.Word.CONTENT_URI, // the user dictionary content URI mNewValues // the values to insert); 新行的数据会进入单个 ContentValues 对象中，该对象在形式上与单行游标类似。 此对象中的列不需要具有相同的数据类型，如果您不想指定值，则可以使用 ContentValues.putNull() 将列设置为 null。 代码段不会添加 _ID 列，因为系统会自动维护此列。 提供程序会向添加的每个行分配唯一的 _ID 值。 通常，提供程序会将此值用作表的主键。 newUri 中返回的内容 URI 会按照以下格式标识新添加的行： 1content://user_dictionary/words/&lt;id_value&gt; &lt;id_value&gt; 是新行的 _ID 内容。 大多数提供程序都能自动检测这种格式的内容 URI，然后在该特定行上执行请求的操作。 要从返回的 Uri 中获取 _ID 的值，请调用 ContentUris.parseId()。 更新数据要更新行，请按照执行插入的方式使用具有更新值的 ContentValues 对象，并按照执行查询的方式使用选择条件。 您使用的客户端方法是 ContentResolver.update()。您只需将值添加至您要更新的列的 ContentValues 对象。 如果您要清除列的内容，请将值设置为 null。 以下代码段会将语言区域具有语言“en”的所有行的语言区域更改为 null。 返回值是已更新的行数： 1234567891011121314151617181920212223// Defines an object to contain the updated valuesContentValues mUpdateValues = new ContentValues();// Defines selection criteria for the rows you want to updateString mSelectionClause = UserDictionary.Words.LOCALE + "LIKE ?";String[] mSelectionArgs = &#123;"en_%"&#125;;// Defines a variable to contain the number of updated rowsint mRowsUpdated = 0;.../* * Sets the updated value and updates the selected words. */mUpdateValues.putNull(UserDictionary.Words.LOCALE);mRowsUpdated = getContentResolver().update( UserDictionary.Words.CONTENT_URI, // the user dictionary content URI mUpdateValues // the columns to update mSelectionClause // the column to select on mSelectionArgs // the value to compare to); 您还应该在调用 ContentResolver.update() 时检查用户输入。如需了解有关此内容的更多详情，请阅读防止恶意输入部分。 删除数据删除行与检索行数据类似：为要删除的行指定选择条件，客户端方法会返回已删除的行数。 以下代码段会删除应用 ID 与“用户”匹配的行。该方法会返回已删除的行数。 123456789101112131415// Defines selection criteria for the rows you want to deleteString mSelectionClause = UserDictionary.Words.APP_ID + " LIKE ?";String[] mSelectionArgs = &#123;"user"&#125;;// Defines a variable to contain the number of rows deletedint mRowsDeleted = 0;...// Deletes the words that match the selection criteriamRowsDeleted = getContentResolver().delete( UserDictionary.Words.CONTENT_URI, // the user dictionary content URI mSelectionClause // the column to select on mSelectionArgs // the value to compare to); 您还应该在调用 ContentResolver.delete() 时检查用户输入。如需了解有关此内容的更多详情，请阅读防止恶意输入部分。 提供程序数据类型内容提供程序可以提供多种不同的数据类型。用户字典提供程序仅提供文本，但提供程序也能提供以下格式： 整型 长整型（长） 浮点型 长浮点型（双倍）提供程序经常使用的另一种数据类型是作为 64KB 字节的数组实施的二进制大型对象 (BLOB)。 您可以通过查看 Cursor 类“获取”方法看到可用数据类型。 提供程序文档中通常都列出了其每个列的数据类型。 用户字典提供程序的数据类型列在其协定类 UserDictionary.Words 参考文档中（协定类部分对协定类进行了介绍）。 您也可以通过调用 Cursor.getType() 来确定数据类型。 提供程序还会维护其定义的每个内容 URI 的 MIME（多用途互联网邮件扩展）数据类型信息。您可以使用 MIME 类型信息查明应用是否可以处理提供程序提供的数据，或根据 MIME 类型选择处理类型。 在使用包含复杂数据结构或文件的提供程序时，通常需要 MIME 类型。 例如，联系人提供程序中的 ContactsContract.Data 表会使用 MIME 类型来标记每行中存储的联系人数据类型。 要获取与内容 URI 对应的 MIME 类型，请调用 ContentResolver.getType()。 MIME 类型引用部分介绍了标准和自定义 MIME 类型的语法。 提供程序访问的替代形式提供程序访问的三种替代形式在应用开发过程中十分重要： 批量访问：您可以通过 ContentProviderOperation 类中的方法创建一批访问调用，然后通过 ContentResolver.applyBatch() 应用它们。 异步查询：您应该在单独线程中执行查询。执行此操作的方式之一是使用 CursorLoader 对象。 加载器指南中的示例展示了如何执行此操作。 通过 Intent 访问数据：尽管您无法直接向提供程序发送 Intent，但可以向提供程序的应用发送 Intent，后者通常具有修改提供程序数据的最佳配置。下文将介绍通过 Intent 进行的批量访问和修改。 批量访问批量访问提供程序适用于插入大量行，或通过同一方法调用在多个表中插入行，或者通常用于跨进程界限将一组操作作为事务处理（原子操作）执行。 要在“批量模式”下访问提供程序， 您可以创建 ContentProviderOperation 对象数组，然后使用 ContentResolver.applyBatch() 将其分派给内容提供程序。您需将内容提供程序的授权传递给此方法，而不是特定内容 URI。 这样可使数组中的每个 ContentProviderOperation 对象都能适用于其他表。 调用 ContentResolver.applyBatch() 会返回结果数组。 ContactsContract.RawContacts 协定类 的说明包括展示批量注入的代码段。 联系人管理器示例应用包含在其 ContactAdder.java 源文件中进行批量访问的示例。 使用帮助程序应用显示数据如果您的应用具有访问权限，您可能仍想使用 Intent 对象在其他应用中显示数据。 例如，日历应用接受 ACTION_VIEW Intent 对象，用于显示特定的日期或事件。 这样，您可以在不创建自己的 UI 的情况下显示日历信息。 如需了解有关此功能的更多信息，请参见日历提供程序指南。 您向其发送 Intent 对象的应用不必是与提供程序关联的应用。 例如，您可以从联系人提供程序中检索联系人，然后将包含联系人图像的内容 URI 的 ACTION_VIEW Intent 发送至图像查看器。 通过 Intent 访问数据Intent 可以提供对内容提供程序的间接访问。即使您的应用不具备访问权限，您也可以通过以下方式允许用户访问提供程序中的数据：从具有权限的应用中获取回结果 Intent，或者通过激活具有权限的应用，然后让用户在其中工作。 通过临时权限获取访问权限即使您没有适当的访问权限，也可以通过以下方式访问内容提供程序中的数据：将 Intent 发送至具有权限的应用，然后接收回包含“URI”权限的结果 Intent。 这些是特定内容 URI 的权限，将持续至接收该权限的 Activity 结束。 具有永久权限的应用将通过在结果 Intent 中设置标志来授予临时权限： 读取权限： FLAG_GRANT_READ_URI_PERMISSION 写入权限： FLAG_GRANT_WRITE_URI_PERMISSION注：这些标志不会为其授权包含在内容 URI 中的提供程序提供常规的读取或写入访问权限。 访问权限仅适用于 URI 本身。 提供程序使用 元素的 android:grantUriPermission 属性以及 元素的 子元素在其清单文件中定义内容 URI 的 URI 权限。安全与权限指南中“URI 权限”部分更加详细地说明了 URI 权限机制。 例如，即使您没有 READ_CONTACTS 权限，也可以在联系人提供程序中检索联系人的数据。您可能希望在向联系人发送电子生日祝福的应用中执行此操作。 您更愿意让用户控制应用所使用的联系人，而不是请求 READ_CONTACTS，让您能够访问用户的所有联系人及其信息。 要执行此操作，您需要使用以下进程： 1.您的应用会使用方法 startActivityForResult() 发送包含操作 ACTION_PICK 和“联系人”MIME 类型 CONTENT_ITEM_TYPE 的 Intent 对象。 2.由于此 Intent 与“联系人”应用的“选择” Activity 的 Intent 过滤器相匹配，因此 Activity 会显示在前台。 3.在选择 Activity 中，用户选择要更新的联系人。 发生此情况时，选择 Activity 会调用 setResult(resultcode, intent) 以设置用于返回至应用的 Intent。Intent 包含用户选择的联系人的内容 URI，以及“extra”标志 FLAG_GRANT_READ_URI_PERMISSION。这些标志会为您的应用授予读取内容 URI 所指向联系人数据的 URI 权限。 然后，选择 Activity 会调用 finish()， 将控制权交还给您的应用。 4.您的 Activity 会返回至前台，系统会调用您的 Activity 的 onActivityResult() 方法。此方法会收到“联系人”应用中选择 Activity 所创建的结果 Intent。 5.通过来自结果 Intent 的内容 URI，您可以读取来自联系人提供程序的联系人数据，即使您未在清单文件中请求对该提供程序的永久读取访问权限。 您可以获取联系人的生日信息或其电子邮件地址，然后发送电子祝福。 使用其他应用允许用户修改您无权访问的数据的简单方法是激活具有权限的应用，让用户在其中执行工作。 例如，日历应用接受 ACTION_INSERT Intent 对象，这让您可以激活 应用的插入 UI。您可以在此 Intent（应用将使用该 Intent 来预先填充 UI）中传递“extra”数据。 由于定期事件具有复杂的语法，因此将事件插入日历提供程序的首选方式是激活具有 ACTION_INSERT 的日历应用，然后让用户在其中插入事件。 协定类协定类定义帮助应用使用内容 URI、列名称、 Intent 操作以及内容提供程序的其他功能的常量。 协定类未自动包含在提供程序中；提供程序的开发者需要定义它们，然后使其可用于其他开发者。 Android 平台中包含的许多提供程序都在软件包 android.provider 中具有对应的协定类。 例如，用户字典提供程序具有包含内容 URI 和列名称常量的协定类 UserDictionary。 “字词”表的内容 URI 在常量 UserDictionary.Words.CONTENT_URI 中定义。 UserDictionary.Words 类也包含列名称常量，本指南的示例代码段中就使用了该常量。 例如，查询投影可以定义为： 123456String[] mProjection =&#123; UserDictionary.Words._ID, UserDictionary.Words.WORD, UserDictionary.Words.LOCALE&#125;; 联系人提供程序的 ContactsContract 也是一个协定类。 此类的参考文档包括示例代码段。其子类之一 ContactsContract.Intents.Insert 是包含 Intent 和 Intent 数据的协定类。 MIME 类型引用内容提供程序可以返回标准 MIME 媒体类型和/或自定义 MIME 类型字符串。 MIME 类型具有格式 1type/subtype 例如，众所周知的 MIME 类型 text/html 具有 text 类型和 html 子类型。如果提供程序为 URI 返回此类型，则意味着使用该 URI 的查询会返回包含 HTML 标记的文本。 自定义 MIME 类型字符串（也称为“特定于供应商”的 MIME 类型）具有更加复杂的类型和子类型值。 类型值始终为 1vnd.android.cursor.dir （多行）或 1vnd.android.cursor.item （单行）。 子类型特定于提供程序。Android 内置提供程序通常具有简单的子类型。 例如，当“通讯录”应用为电话号码创建行时，它会在行中设置以下 MIME 类型： 1vnd.android.cursor.item/phone_v2 请注意，子类型值只是 phone_v2。 其他提供程序开发者可能会根据提供程序的授权和表名称创建自己的子类型模式。 例如，假设提供程序包含列车时刻表。 提供程序的授权是 com.example.trains，并包含表 Line1、Line2 和 Line3。在响应表 Line1 的内容 URI 1content://com.example.trains/Line1 时，提供程序会返回 MIME 类型 1vnd.android.cursor.dir/vnd.example.line1 在响应表 Line2 中第 5 行的内容 URI 1content://com.example.trains/Line2/5 时，提供程序会返回 MIME 类型 vnd.android.cursor.item/vnd.example.line2大多数内容提供程序都会为其使用的 MIME 类型定义协定类常量。例如，联系人提供程序协定类 ContactsContract.RawContacts 会为单个原始联系人行的 MIME 类型定义常量 CONTENT_ITEM_TYPE。 内容 URI 部分介绍了单个行的内容 URI。]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>ContentProvider</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android组件之BroadcastReceiver]]></title>
    <url>%2Fandroid_broadcastreceiver%2F</url>
    <content type="text"><![CDATA[简述Android应用可以发送或接收来自Android系统和其他Android应用的广播消息，类似于 发布 - 订阅 设计模式。这些广播是在感兴趣的事件发生时发送的。例如，Android系统在发生各种系统事件时发送广播，例如系统启动或设备开始充电时。应用程序还可以发送自定义广播，例如，向其他应用程序通知他们可能感兴趣的内容（例如，某些新数据已被下载）。 应用可以注册以接收特定的广播。发送广播时，系统会自动将广播路由到已预订接收该特定广播类型的应用。 一般来说，广播可以用作应用程序之间和正常用户流程之外的消息传递系统。但是，您必须小心，不要滥用在后台响应广播和运行作业，这可能会导致系统性能降低。 关于系统广播系统在发生各种系统事件时自动发送广播，例如系统切入和切出飞行模式。系统广播被发送到订阅接收事件的所有应用程序。 广播消息本身包装在一个Intent对象中,该对象的操作字符串标识发生的事件（例如 android.intent.action.AIRPLANE_MODE）。Intent还可能包括捆绑到其额外领域的附加信息。例如，飞机模式意图包含一个布尔额外值，表示飞行模式是否打开。 有关系统广播操作的完整列表，请参阅BROADCAST_ACTIONS.TXT Android SDK中的 文件。每个广播动作都有一个与其相关的常量字段。例如，常数的值 ACTION_AIRPLANE_MODE_CHANGED是 android.intent.action.AIRPLANE_MODE。每个广播操作的文档可在其关联的常量字段中找到。 系统广播的变化（2018/05/21 修改）Android 7.0及更高版本不再发送以下系统广播。此优化会影响所有应用，而不仅仅是针对Android 7.0的应用。 ACTION_NEW_PICTUREACTION_NEW_VIDEO定位到Android 7.0（API级别24）及更高版本的应用必须通过以下方式注册以下广播registerReceiver(BroadcastReceiver, IntentFilter)。在清单中声明接收者不起作用。 CONNECTIVITY_ACTION从Android 8.0（API级别26）开始，系统对清单声明的接收方施加额外的限制。如果您的应用定位到API级别26或更高，则无法使用清单来声明大多数隐式广播的接收方（专门针对您的应用的广播）。当用户主动使用您的应用程序时，您仍然可以使用Context上下文注册的接收器。 广播的接收应用程序可以通过两种方式接收广播：通过清单Manifest声明的接收器和上下文Context注册的接收器。 清单文件Manifest声明的接收者如果您在清单中声明广播接收器，系统会在广播发送时启动您的应用程序（如果应用程序尚未运行）。注意：如果您的应用程序的目标API级别为26或更高，则不能使用清单来声明隐式广播的接收器（专门不针对您的应用程序的广播），但可以免除该限制的一些隐式广播除外。 要在清单中声明广播接收器，请执行以下步骤： 1. 在应用程序的清单中指定元素。 123456&lt;receiver android:name=".MyBroadcastReceiver" android:exported="true"&gt; &lt;intent-filter&gt; &lt;action android:name="android.intent.action.BOOT_COMPLETED"/&gt; &lt;action android:name="android.intent.action.INPUT_METHOD_CHANGED" /&gt; &lt;/intent-filter&gt;&lt;/receiver&gt; 意图过滤器指定您的接收器订阅的广播操作。 2.子类BroadcastReceiver和实现onReceive(Context, Intent)。以下示例中的广播接收机会记录并显示广播的内容： 123456789101112public class MyBroadcastReceiver extends BroadcastReceiver &#123; private static final String TAG = "MyBroadcastReceiver"; @Override public void onReceive(Context context, Intent intent) &#123; StringBuilder sb = new StringBuilder(); sb.append("Action: " + intent.getAction() + "\n"); sb.append("URI: " + intent.toUri(Intent.URI_INTENT_SCHEME).toString() + "\n"); String log = sb.toString(); Log.d(TAG, log); Toast.makeText(context, log, Toast.LENGTH_LONG).show(); &#125;&#125; 系统包管理器在安装应用程序时注册接收器。接收器然后成为您的应用程序的单独入口点，这意味着系统可以启动应用程序并在应用程序当前未运行时传送广播。 系统创建一个新的BroadcastReceiver组件对象来处理它收到的每个广播。此对象仅在调用期间有效onReceive(Context, Intent)。一旦您的代码从此方法返回，系统会认为该组件不再处于活动状态。 上下文Context注册的接收器要使用上下文注册接收器，请执行以下步骤： 1.创建一个实例BroadcastReceiver。 1BroadcastReceiver br = new MyBroadcastReceiver(); 2.通过以下方式创建IntentFilter并注册接收器registerReceiver(BroadcastReceiver, IntentFilter)： 123IntentFilter filter = new IntentFilter(ConnectivityManager.CONNECTIVITY_ACTION);filter.addAction(Intent.ACTION_AIRPLANE_MODE_CHANGED);this.registerReceiver(br, filter); 3.要停止接收广播，请调用unregisterReceiver(android.content.BroadcastReceiver)。当您不再需要接收器或上下文不再有效时，请务必注销接收器。 注意你注册和注销接收者的位置，例如，如果你注册一个接收者来onCreate(Bundle)使用活动的上下文，你应该取消注册，onDestroy()以防止接收者从活动上下文中泄漏出去。如果你注册一个接收器onResume()，你应该取消注册，onPause()以防止多次注册（如果你不想在暂停时收到广播，这可以减少不必要的系统开销）。不要在onSaveInstanceState(Bundle)中取消注册，因为如果用户移回到历史堆栈中，则不会调用它。 对进程状态的影响您的BroadcastReceiver（无论是否在运行）会影响其包含进程的状态，从而影响其被系统杀死的可能性。例如，当一个进程执行一个接收者（也就是说，当前在其onReceive() 方法中运行该代码）时，它被认为是一个前台进程。除非存在极大的内存压力，否则系统会继续运行。 但是，一旦代码返回onReceive()，BroadcastReceiver不再处于活动状态。接收者的宿主进程变得与其中运行的其他应用程序组件一样重要。如果该进程仅托管清单声明的接收方（用户从未或最近未与之交互过的应用程序的常见情况），则在返回时onReceive()，系统将其进程视为低优先级进程，并可能会将其终止为其他更重要的流程提供资源。 出于这个原因，您不应该从广播接收器开始长时间运行后台线程。在onReceive()执行后，系统可以随时终止进程以回收内存，并且这样做会终止进程中运行的衍生线程。为避免这种情况，您应该调用goAsync()（如果您希望有更多时间在后台线程中处理广播），或者JobService使用接收者从接收者调度一个JobScheduler，因此系统知道该过程继续执行主动工作。有关更多信息，请参阅进程和应用程序生命周期。 下面的代码片段展示BroadcastReceiver使用goAsync()来完成更多的任务在onReceive()中。如果你想在你的工作中完成的工作onReceive()足够长，导致UI线程错过一帧（&gt; 16ms），这种情况更适合后台线程，这是特别有用的。 123456789101112131415161718192021public class MyBroadcastReceiver extends BroadcastReceiver &#123; private static final String TAG = "MyBroadcastReceiver"; @Override public void onReceive(final Context context, final Intent intent) &#123; final PendingResult pendingResult = goAsync(); AsyncTask&lt;String, Integer, String&gt; asyncTask = new AsyncTask&lt;String, Integer, String&gt;() &#123; @Override protected String doInBackground(String... params) &#123; StringBuilder sb = new StringBuilder(); sb.append("Action: " + intent.getAction() + "\n"); sb.append("URI: " + intent.toUri(Intent.URI_INTENT_SCHEME).toString() + "\n"); Log.d(TAG, log); // Must call finish() so the BroadcastReceiver can be recycled. pendingResult.finish(); return data; &#125; &#125;; asyncTask.execute(); &#125;&#125; 发送广播Android为应用程序发送广播提供了三种方式： sendOrderedBroadcast(Intent, String) 方法一次向一个接收器发送广播。当每个接收器依次执行时，它可以将结果传播到下一个接收器，或者它可以完全中止广播，使其不会传递给其他接收器。运行的接收器的执行顺序可以通过匹配意图过滤器的android：priority属性进行控制; 具有相同优先级的接收器将以任意顺序运行。 sendBroadcast(Intent)方法以未定义的顺序向所有接收机发送广播。这被称为普通广播。这样更有效率，但意味着接收器无法读取其他接收器的结果，传播从广播接收的数据或中止广播。 LocalBroadcastManager.sendBroadcast方法将广播发送到与发件人位于同一应用程序中的接收者。如果您不需要跨应用程序发送广播，请使用本地广播。实现效率更高（不需要进程间通信），您无需担心与其他应用程序能够接收或发送广播相关的任何安全问题。以下代码片段演示了如何通过创建一个Intent并调用来发送广播sendBroadcast(Intent)。 1234Intent intent = new Intent();intent.setAction("com.example.broadcast.MY_NOTIFICATION");intent.putExtra("data","Notice me senpai!");sendBroadcast(intent); 广播消息被包裹在一个Intent对象中。意图的动作字符串必须提供应用程序的Java包名称语法并唯一标识广播事件。您可以将附加信息附加到意图putExtra(String, Bundle)。您也可以通过调用setPackage(String)意图将广播限制在同一组织中的一组应用程序。 带有权限限制的广播权限允许您将广播限制为拥有特定权限的应用程序集。您可以对广播的发送者或接收者实施限制。 发送广播时附带权限当你打调用sendBroadcast(Intent, String)或者 sendOrderedBroadcast(Intent, String, BroadcastReceiver, Handler, int, String, Bundle)，你可以指定一个权限参数。只有接收方已经请求了该许可在他们的清单中标记（并且随后在危险时被授予许可）可以接收广播。例如，下面的代码发送一个广播： 12sendBroadcast(new Intent("com.example.NOTIFY"), Manifest.permission.SEND_SMS); 要接收广播，接收应用程序必须请求权限，如下所示： 1&lt;uses-permission android:name="android.permission.SEND_SMS"/&gt; 您可以指定现有的系统权限SEND_SMS或使用该元素定义自定义权限 。注意：自定义权限是在安装应用程序时注册的。定义自定义权限的应用程序必须安装在使用它的应用程序之前。 接收带有权限的广播如果您在注册广播接收机时指定一个权限参数（无论是在清单registerReceiver(BroadcastReceiver, IntentFilter, String, Handler)中的标签还是在 标签中），那么只有在其清单中通过标签请求权限的广播公司 （如果它是危险的，随后被授予权限）可以发送接收者的意图。 例如，假设您的接收应用程序具有清单声明的接收方，如下所示： 123456&lt;receiver android:name=".MyBroadcastReceiver" android:permission="android.permission.SEND_SMS"&gt; &lt;intent-filter&gt; &lt;action android:name="android.intent.action.AIRPLANE_MODE"/&gt; &lt;/intent-filter&gt;&lt;/receiver&gt; 或者您的接收应用程序有一个上下文注册的接收器，如下所示： 12IntentFilter filter = new IntentFilter(Intent.ACTION_AIRPLANE_MODE_CHANGED);registerReceiver(receiver, filter, Manifest.permission.SEND_SMS, null ); 然后，为了能够向这些接收方发送广播，发送应用程序必须请求权限，如下所示： 1&lt;uses-permission android:name="android.permission.SEND_SMS"/&gt; 安全考虑和最佳实践以下是发送和接收广播的一些安全考虑事项和最佳做法： 如果您不需要将广播发送到您的应用以外的组件，则LocalBroadcastManager可以使用支持库中的可用 广播发送和接收本地广播。的LocalBroadcastManager效率要高得多（不需要进程间通信），并可以让你避免考虑与其他应用程序能够接收或发送你的广播任何安全问题。本地广播可以在您的应用程序中用作通用的发布/订阅事件总线，而无需系统广播的任何开销。 如果许多应用程序已注册接收清单中的相同广播，则可能会导致系统启动大量应用程序，从而对设备性能和用户体验产生重大影响。为了避免这种情况，优先使用清单声明上的注册。有时，Android系统本身会强制使用上下文注册的接收器。例如，CONNECTIVITY_ACTION广播仅传送给上下文注册的接收器。 不要使用隐含的意图广播敏感信息。任何注册的应用程序都可以读取信息以接收广播。有三种方法可以控制谁可以接收您的广播： 您可以在发送广播时指定权限。在Android 4.0及更高版本，可以指定一个 包与 setPackage(String)发送广播时。系统将广播限制为与包匹配的一组应用程序。您可以发送本地广播LocalBroadcastManager。当您注册接收器时，任何应用程序都可能将潜在的恶意广播发送到您应用的接收器。有三种方法可以限制您的应用收到的广播： 您可以在注册广播接收机时指定权限。对于清单声明的接收者，您可以在清单中将 android：exported 属性设置为“false”。接收器不接收来自应用程序外部的广播。你可以限制自己只有本地广播LocalBroadcastManager。广播操作的命名空间是全局的。确保动作名称和其他字符串被写入您拥有的名称空间中，否则您可能会无意中与其他应用程序发生冲突。 因为接收者的onReceive(Context, Intent)方法在主线程上运行，所以它应该快速执行并返回。如果您需要执行长时间运行的工作，请孵化线程或启动后台服务，因为系统可能会在onReceive()返回后终止整个进程 。建议：调用goAsync()接收者的onReceive()方法并传递BroadcastReceiver.PendingResult给后台线程。这使广播从返回后保持活动状态onReceive()。但是，即使采用这种方法，系统也希望您能够很快完成广播（不到10秒）。它确实允许您将工作移至另一个线程以避免妨碍主线程。用JobScheduler来协调任务。 不要从广播接收者中开一一个新的Activity，因为用户体验很糟糕; 特别是如果有多个接收器的话。相反，请考虑使用Notification来显示通知。]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>BroadcastReceiver</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android组件之Service]]></title>
    <url>%2Fandroid_service%2F</url>
    <content type="text"><![CDATA[简述Service是Android中一个类，它是Android四大组件之一，使用Service可以在后台执行长时间的操作（ perform long-running operations in the background ），Service并不与用户产生UI交互。其他的应用组件可以启动Service，即便用户切换了其他应用，启动的Service仍可在后台运行。一个组件可以与Service绑定并与之交互，甚至是跨进程通信（IPC）。例如，一个Service可以在后台执行网络请求、播放音乐、执行文件读写操作或者与 content provider交互 等。 Service的解读A Service is not a separate process. The Service object itself does not imply it is running in its own process; unless otherwise specified, it runs in the same process as the application it is part of.A Service is not a thread. It is not a means itself to do work off of the main thread (to avoid Application Not Responding errors).服务并不是一个独立的进程，服务对象本身并不意味着它在自己的进程中运行，除非另有说明，否者它与所属的应用在同一进程工作。服务也不是一个独立的线程，它并不是为了避免因耗时任务造成无响应而设计的。 Thus a Service itself is actually very simple, providing two main features: A facility for the application to tell the system about something it wants to be doing in the background (even when the user is not directly interacting with the application). This corresponds to calls to Context.startService(), which ask the system to schedule work for the service, to be run until the service or someone else explicitly stop it.A facility for an application to expose some of its functionality to other applications. This corresponds to calls to Context.bindService(), which allows a long-standing connection to be made to the service in order to interact with it.因此，服务本身其实非常简单，提供了两种主要功能：一种是应用希望在后台做一些事情而且不直接与应用程序交互，这种场景可以通过Context.startService()来启动一个服务，服务开始工作，直到系统或用户明确的停止它的执行。另一种是需要将服务执行的过程或结果以及某些功能展示给其他组件时，这种场景需要进行服务的绑定，通过Context.bindService()，以便与服务进行长期的连接与其进行交互。 无论通过那种方式启动service（start、bind、start&amp;bind），任何组件（甚至其他应用的组件）都可以使用service。并通过Intent传递参数。当然，您也可以将Service在manifest文件中配置成私有的，不允许其他应用访问。 12345678910&lt;service android:enabled=["true" | "false"] android:exported=["true" | "false"] android:icon="drawable resource" android:isolatedProcess=["true" | "false"] android:label="string resource" android:name="string" android:permission="string" android:process="string" &gt; . . .&lt;/service&gt; ● android:exported：代表是否能被其他应用隐式调用，其默认值是由service中有无intent-filter决定的，如果有intent-filter，默认值为true，否则为false。为false的情况下，即使有intent-filter匹配，也无法打开，即无法被其他应用隐式调用。 ● android:name：对应Service类名 ● android:permission：是权限声明 ● android:process：是否需要在单独的进程中运行,当设置为android:process=”:remote”时，代表Service在单独的进程中运行。注意“：”很重要，它的意思是指要在当前进程名称前面附加上当前的包名，所以“remote”和”:remote”不是同一个意思，前者的进程名称为：remote，而后者的进程名称为：App-packageName:remote。 ● android:isolatedProcess ：设置 true 意味着，服务会在一个特殊的进程下运行，这个进程与系统其他进程分开且没有自己的权限。与其通信的唯一途径是通过服务的API(bind and start)。 ● android:enabled：是否可以被系统实例化，默认为 true因为父标签 也有 enable 属性，所以必须两个都为默认值 true 的情况下服务才会被激活，否则不会激活。 创建一个服务12345678910111213141516171819202122232425262728293031323334353637383940414243444546public class SimpleService extends Service &#123; /** * 必须实现的方法 * 绑定服务时调用 * @param intent * @return */ @Nullable @Override public IBinder onBind(Intent intent) &#123; return null; &#125; /** * 首次创建服务时，系统将调用此方法来执行一次性设置程序（在调用 onStartCommand() 或 onBind() 之前）。 * 如果服务已在运行，则不会调用此方法。该方法只被调用一次 */ @Override public void onCreate() &#123; System.out.println("onCreate invoke"); super.onCreate(); &#125; /** * 每次通过startService()方法启动Service时都会被回调。 * @param intent * @param flags * @param startId * @return */ @Override public int onStartCommand(Intent intent, int flags, int startId) &#123; System.out.println("onStartCommand invoke"); return super.onStartCommand(intent, flags, startId); &#125; /** * 服务销毁时的回调 */ @Override public void onDestroy() &#123; System.out.println("onDestroy invoke"); super.onDestroy(); &#125;&#125; 在服务类中，onBind方法是必须要重写的一个方法。但是如果您不需要进行绑定服务，无需对其进行实现，返回null即可，如果您需要绑定服务，则必须对其进行相应实现。一个服务的完整声明周期是onCreate()，onStartCommand()，onDestroy()。 主要方法的解读 onCreate():首次创建服务时会调用该方法，如果服务已经存在了，则不会收到该方法的回调。 onStartCommand(Intent intent, int flags, int startId):当我们在Activity中调用startService方法时，该方法会被调用。与其他方法不同的是，多次调用startService时onStartCommand均会被调用。参数解读：intent ：启动时，启动组件传递过来的Intent，如Activity可利用Intent封装所需要的参数并传递给Service flags：表示启动请求时是否有额外数据，可选值有 0，START_FLAG_REDELIVERY，START_FLAG_RETRY，0代表没有，它们具体含义如下： START_FLAG_REDELIVERY这个值代表了onStartCommand方法的返回值为START_REDELIVER_INTENT，而且在上一次服务被杀死前会去调用stopSelf方法停止服务。其中START_REDELIVER_INTENT意味着当Service因内存不足而被系统kill后，则会重建服务，并通过传递给服务的最后一个 Intent 调用 onStartCommand()，此时Intent时有值的。 START_FLAG_RETRY该flag代表当onStartCommand调用后一直没有返回值时，会尝试重新去调用onStartCommand()。 startId ： 指明当前服务的唯一ID，与stopSelfResult (int startId)配合使用，stopSelfResult 可以更安全地根据ID停止服务。 该方法有个int类型的返回值，这是本方法的关键点。返回值有三种取值：START_STICKY和START_NOT_STICKY以及START_REDELIVER_INTENT。 START_STICKY 当Service因内存不足而被系统kill后，一段时间后内存再次空闲时，系统将会尝试重新创建此Service，一旦创建成功后将回调onStartCommand方法，但其中的Intent将是null，除非有挂起的Intent，如pendingintent，这个状态下比较适用于不执行命令、但无限期运行并等待作业的媒体播放器或类似服务。 START_NOT_STICKY 当Service因内存不足而被系统kill后，即使系统内存再次空闲时，系统也不会尝试重新创建此Service。除非程序中再次调用startService启动此Service，这是最安全的选项，可以避免在不必要时以及应用能够轻松重启所有未完成的作业时运行服务。 START_REDELIVER_INTENT 当Service因内存不足而被系统kill后，则会重建服务，并通过传递给服务的最后一个 Intent 调用 onStartCommand()，任何挂起 Intent均依次传递。与START_STICKY不同的是，其中的传递的Intent将是非空，是最后一次调用startService中的intent。这个值适用于主动执行应该立即恢复的作业（例如下载文件）的服务。 由于每次启动服务（调用startService）时，onStartCommand方法都会被调用，因此我们可以通过该方法使用Intent给Service传递所需要的参数，然后在onStartCommand方法中处理的事件，最后根据需求选择不同的Flag返回值，以达到对程序更友好的控制。 onDestroy():当服务内部调用stopSelf()或者调用stopService()时，服务被销毁时会执行onDestroy()方法。 启动一个服务12Intent intent = new Intent(this, TestService.class);startService(intent); 通过startService启动的服务，服务启动后，该服务和启动它的组件没有任何关联，服务便长期运行于后台，即便启动它的组件销毁，服务依然是存在的，当需要明确的停止服务时，需要调用stopService()。 绑定一个服务当需要进行服务的绑定时，首先在服务类中必须要实现onBind()方法返回一个binder对象。与非绑定的启动服务不同的是，启动服务时只需要重写该方法并不需要返回IBinder接口的实现类对象。IBinder接口其实是我们与服务通信的桥梁。可以使用三种方法定义IBinder接口。 本地服务扩展 Binder 类 如果服务是提供给自有应用专用的，并且Service(服务端)与客户端相同的进程中运行（常见情况），则应通过扩展 Binder 类并从 onBind() 返回它的一个实例来创建接口。客户端收到 Binder 后，可利用它直接访问 Binder 实现中以及Service 中可用的公共方法。如果我们的服务只是自有应用的后台工作线程，则优先采用这种方法。 不采用该方式创建接口的唯一原因是，服务被其他应用或不同的进程调用。 使用 Messenger Messenger可以翻译为信使，通过它可以在不同的进程中共传递Message对象(Handler中的Messager，因此 Handler 是 Messenger 的基础)，在Message中可以存放我们需要传递的数据，然后在进程间传递。如果需要让接口跨不同的进程工作，则可使用 Messenger 为服务创建接口，客户端就可利用 Message 对象向服务发送命令。同时客户端也可定义自有 Messenger，以便服务回传消息。这是执行进程间通信 (IPC) 的最简单方法，因为 Messenger 会在单一线程中创建包含所有请求的队列，也就是说Messenger是以串行的方式处理客户端发来的消息，这样我们就不必对服务进行线程安全设计了。 使用 AIDL 由于Messenger是以串行的方式处理客户端发来的消息，如果当前有大量消息同时发送到Service(服务端)，Service仍然只能一个个处理，这也就是Messenger跨进程通信的缺点了，因此如果有大量并发请求，Messenger就会显得力不从心了，这时AIDL（Android 接口定义语言）就派上用场了， 但实际上Messenger 的跨进程方式其底层实现 就是AIDL，只不过android系统帮我们封装成透明的Messenger罢了 。因此，如果我们想让服务同时处理多个请求，则应该使用 AIDL。 在此情况下，服务必须具备多线程处理能力，并采用线程安全式设计。使用AIDL必须创建一个定义编程接口的 .aidl 文件。Android SDK 工具利用该文件生成一个实现接口并处理 IPC 的抽象类，随后可在服务内对其进行扩展。 本地服务扩展Binder类前面描述过，如果我们的服务仅供本地应用使用，不需要跨进程工作，则可以实现自有 Binder 类，让客户端通过该类直接访问服务中的公共方法。其使用开发步骤如下 1.创建BindService服务端，继承自Service并在类中，创建一个实现IBinder 接口的实例对象并提供公共方法给客户端调用2.从 onBind() 回调方法返回此 Binder 实例。3.在客户端中，从 onServiceConnected() 回调方法接收 Binder，并使用提供的方法调用绑定服务。 注意：此方式只有在客户端和服务位于同一应用和进程内才有效，如对于需要将 Activity 绑定到在后台播放音乐的自有服务的音乐应用，此方式非常有效。另一点之所以要求服务和客户端必须在同一应用内，是为了便于客户端转换返回的对象和正确调用其 API。服务和客户端还必须在同一进程内，因为此方式不执行任何跨进程编组。 google官方示例代码： 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475public class LocalService extends Service &#123; private NotificationManager mNM; // Unique Identification Number for the Notification. // We use it on Notification start, and to cancel it. private int NOTIFICATION = R.string.local_service_started; /** * Class for clients to access. Because we know this service always * runs in the same process as its clients, we don't need to deal with * IPC. */ public class LocalBinder extends Binder &#123; LocalService getService() &#123; return LocalService.this; &#125; &#125; @Override public void onCreate() &#123; mNM = (NotificationManager)getSystemService(NOTIFICATION_SERVICE); // Display a notification about us starting. We put an icon in the status bar. showNotification(); &#125; @Override public int onStartCommand(Intent intent, int flags, int startId) &#123; Log.i("LocalService", "Received start id " + startId + ": " + intent); return START_NOT_STICKY; &#125; @Override public void onDestroy() &#123; // Cancel the persistent notification. mNM.cancel(NOTIFICATION); // Tell the user we stopped. Toast.makeText(this, R.string.local_service_stopped, Toast.LENGTH_SHORT).show(); &#125; @Override public IBinder onBind(Intent intent) &#123; return mBinder; &#125; // This is the object that receives interactions from clients. See // RemoteService for a more complete example. private final IBinder mBinder = new LocalBinder(); /** * Show a notification while this service is running. */ private void showNotification() &#123; // In this sample, we'll use the same text for the ticker and the expanded notification CharSequence text = getText(R.string.local_service_started); // The PendingIntent to launch our activity if the user selects this notification PendingIntent contentIntent = PendingIntent.getActivity(this, 0, new Intent(this, LocalServiceActivities.Controller.class), 0); // Set the info for the views that show in the notification panel. Notification notification = new Notification.Builder(this) .setSmallIcon(R.drawable.stat_sample) // the status icon .setTicker(text) // the status text .setWhen(System.currentTimeMillis()) // the time stamp .setContentTitle(getText(R.string.local_service_label)) // the label of the entry .setContentText(text) // the contents of the entry .setContentIntent(contentIntent) // The intent to send when the entry is clicked .build(); // Send the notification. mNM.notify(NOTIFICATION, notification); &#125;&#125; 在Activity中进行服务的绑定操作： 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667686970/** * Example of binding and unbinding to the local service. * bind to, receiving an object through which it can communicate with the service. * * Note that this is implemented as an inner class only keep the sample * all together; typically this code would appear in some separate class. */public static class Binding extends Activity &#123; // Don't attempt to unbind from the service unless the client has received some // information about the service's state. private boolean mShouldUnbind; // To invoke the bound service, first make sure that this value // is not null. private LocalService mBoundService; private ServiceConnection mConnection = new ServiceConnection() &#123; public void onServiceConnected(ComponentName className, IBinder service) &#123; // This is called when the connection with the service has been // established, giving us the service object we can use to // interact with the service. Because we have bound to a explicit // service that we know is running in our own process, we can // cast its IBinder to a concrete class and directly access it. mBoundService = ((LocalService.LocalBinder)service).getService(); // Tell the user about this for our demo. Toast.makeText(Binding.this, R.string.local_service_connected, Toast.LENGTH_SHORT).show(); &#125; public void onServiceDisconnected(ComponentName className) &#123; // This is called when the connection with the service has been // unexpectedly disconnected -- that is, its process crashed. // Because it is running in our same process, we should never // see this happen. mBoundService = null; Toast.makeText(Binding.this, R.string.local_service_disconnected, Toast.LENGTH_SHORT).show(); &#125; &#125;; void doBindService() &#123; // Attempts to establish a connection with the service. We use an // explicit class name because we want a specific service // implementation that we know will be running in our own process // (and thus won't be supporting component replacement by other // applications). if (bindService(new Intent(Binding.this, LocalService.class), mConnection, Context.BIND_AUTO_CREATE)) &#123; mShouldUnbind = true; &#125; else &#123; Log.e("MY_APP_TAG", "Error: The requested service doesn't " + "exist, or this client isn't allowed access to it."); &#125; &#125; void doUnbindService() &#123; if (mShouldUnbind) &#123; // Release information about the service's state. unbindService(mConnection); mShouldUnbind = false; &#125; &#125; @Override protected void onDestroy() &#123; super.onDestroy(); doUnbindService(); &#125;&#125; 在客户端中我们创建了一个ServiceConnection对象，该代表与服务的连接，它只有两个方法， onServiceConnected和onServiceDisconnected，其含义如下： onServiceConnected(ComponentName name, IBinder service)系统会调用该方法以传递服务的 onBind() 方法返回的 IBinder。其中service便是服务端返回的IBinder实现类对象，通过该对象我们便可以调用获取LocalService实例对象，进而调用服务端的公共方法。而ComponentName是一个封装了组件(Activity, Service, BroadcastReceiver, or ContentProvider)信息的类，如包名，组件描述等信息，较少使用该参数。 onServiceDisconnected(ComponentName name)Android 系统会在与服务的连接意外中断时（例如当服务崩溃或被终止时）调用该方法。 绑定与解除绑定： bindService(Intent service, ServiceConnection conn, int flags)该方法执行绑定服务操作，其中Intent是我们要绑定的服务(也就是LocalService)的意图，而ServiceConnection代表与服务的连接，它只有两个方法，前面已分析过，flags则是指定绑定时是否自动创建Service。0代表不自动创建、BIND_AUTO_CREATE则代表自动创建。 unbindService(ServiceConnection conn)该方法执行解除绑定的操作，其中ServiceConnection代表与服务的连接。 远程Messenger服务示例如果您需要能够编写一个可以在远程进程中与客户端进行复杂通信的服务（不仅仅是使用 Context.startService向其发送命令），那么您可以使用Messenger该类而不是编写完整的AIDL文件。使用 Messenger 服务提供通信接口，利用此方式，我们无需使用 AIDL 便可执行进程间通信 (IPC)。以下是 Messenger 使用的主要步骤： 1.服务实现一个 Handler，由其接收来自客户端的每个调用的回调 2.Handler 用于创建 Messenger 对象（对 Handler 的引用） 3.Messenger 创建一个 IBinder，服务通过 onBind() 使其返回客户端 4.客户端使用 IBinder 将 Messenger（引用服务的 Handler）实例化，然后使用Messenger将 Message 对象发送给服务 5.服务在其 Handler 中（在 handleMessage() 方法中）接收每个 Message 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116117118119public class MessengerService extends Service &#123; /** For showing and hiding our notification. */ NotificationManager mNM; /** Keeps track of all current registered clients. */ ArrayList&lt;Messenger&gt; mClients = new ArrayList&lt;Messenger&gt;(); /** Holds last value set by a client. */ int mValue = 0; /** * Command to the service to register a client, receiving callbacks * from the service. The Message's replyTo field must be a Messenger of * the client where callbacks should be sent. */ static final int MSG_REGISTER_CLIENT = 1; /** * Command to the service to unregister a client, ot stop receiving callbacks * from the service. The Message's replyTo field must be a Messenger of * the client as previously given with MSG_REGISTER_CLIENT. */ static final int MSG_UNREGISTER_CLIENT = 2; /** * Command to service to set a new value. This can be sent to the * service to supply a new value, and will be sent by the service to * any registered clients with the new value. */ static final int MSG_SET_VALUE = 3; /** * Handler of incoming messages from clients. */ class IncomingHandler extends Handler &#123; @Override public void handleMessage(Message msg) &#123; switch (msg.what) &#123; case MSG_REGISTER_CLIENT: mClients.add(msg.replyTo); break; case MSG_UNREGISTER_CLIENT: mClients.remove(msg.replyTo); break; case MSG_SET_VALUE: mValue = msg.arg1; for (int i=mClients.size()-1; i&gt;=0; i--) &#123; try &#123; mClients.get(i).send(Message.obtain(null, MSG_SET_VALUE, mValue, 0)); &#125; catch (RemoteException e) &#123; // The client is dead. Remove it from the list; // we are going through the list from back to front // so this is safe to do inside the loop. mClients.remove(i); &#125; &#125; break; default: super.handleMessage(msg); &#125; &#125; &#125; /** * Target we publish for clients to send messages to IncomingHandler. */ final Messenger mMessenger = new Messenger(new IncomingHandler()); @Override public void onCreate() &#123; mNM = (NotificationManager)getSystemService(NOTIFICATION_SERVICE); // Display a notification about us starting. showNotification(); &#125; @Override public void onDestroy() &#123; // Cancel the persistent notification. mNM.cancel(R.string.remote_service_started); // Tell the user we stopped. Toast.makeText(this, R.string.remote_service_stopped, Toast.LENGTH_SHORT).show(); &#125; /** * When binding to the service, we return an interface to our messenger * for sending messages to the service. */ @Override public IBinder onBind(Intent intent) &#123; return mMessenger.getBinder(); &#125; /** * Show a notification while this service is running. */ private void showNotification() &#123; // In this sample, we'll use the same text for the ticker and the expanded notification CharSequence text = getText(R.string.remote_service_started); // The PendingIntent to launch our activity if the user selects this notification PendingIntent contentIntent = PendingIntent.getActivity(this, 0, new Intent(this, Controller.class), 0); // Set the info for the views that show in the notification panel. Notification notification = new Notification.Builder(this) .setSmallIcon(R.drawable.stat_sample) // the status icon .setTicker(text) // the status text .setWhen(System.currentTimeMillis()) // the time stamp .setContentTitle(getText(R.string.local_service_label)) // the label of the entry .setContentText(text) // the contents of the entry .setContentIntent(contentIntent) // The intent to send when the entry is clicked .build(); // Send the notification. // We use a string id because it is a unique number. We use it later to cancel. mNM.notify(R.string.remote_service_started, notification); &#125;&#125; 如果你希望服务运行于独立的远程进程中，你需要使用android:process在manifest中进行配置。 12&lt;service android:name=".app.MessengerService" android:process=":remote" /&gt; 绑定服务: 123456789101112131415161718192021222324252627282930313233343536373839404142434445464748495051525354555657585960616263646566676869707172737475767778798081828384858687888990919293949596979899100101102103104105106107108109110111112113114115116117118119120/** * Example of binding and unbinding to the remote service. * This demonstrates the implementation of a service which the client will * bind to, interacting with it through an aidl interface. * * Note that this is implemented as an inner class only keep the sample * all together; typically this code would appear in some separate class. */public static class Binding extends Activity &#123; /** Messenger for communicating with service. */ Messenger mService = null; /** Flag indicating whether we have called bind on the service. */ boolean mIsBound; /** Some text view we are using to show state information. */ TextView mCallbackText; /** * Handler of incoming messages from service. */ class IncomingHandler extends Handler &#123; @Override public void handleMessage(Message msg) &#123; switch (msg.what) &#123; case MessengerService.MSG_SET_VALUE: mCallbackText.setText("Received from service: " + msg.arg1); break; default: super.handleMessage(msg); &#125; &#125; &#125; /** * Target we publish for clients to send messages to IncomingHandler. */ final Messenger mMessenger = new Messenger(new IncomingHandler()); /** * Class for interacting with the main interface of the service. */ private ServiceConnection mConnection = new ServiceConnection() &#123; public void onServiceConnected(ComponentName className, IBinder service) &#123; // This is called when the connection with the service has been // established, giving us the service object we can use to // interact with the service. We are communicating with our // service through an IDL interface, so get a client-side // representation of that from the raw service object. mService = new Messenger(service); mCallbackText.setText("Attached."); // We want to monitor the service for as long as we are // connected to it. try &#123; Message msg = Message.obtain(null, MessengerService.MSG_REGISTER_CLIENT); msg.replyTo = mMessenger; mService.send(msg); // Give it some value as an example. msg = Message.obtain(null, MessengerService.MSG_SET_VALUE, this.hashCode(), 0); mService.send(msg); &#125; catch (RemoteException e) &#123; // In this case the service has crashed before we could even // do anything with it; we can count on soon being // disconnected (and then reconnected if it can be restarted) // so there is no need to do anything here. &#125; // As part of the sample, tell the user what happened. Toast.makeText(Binding.this, R.string.remote_service_connected, Toast.LENGTH_SHORT).show(); &#125; public void onServiceDisconnected(ComponentName className) &#123; // This is called when the connection with the service has been // unexpectedly disconnected -- that is, its process crashed. mService = null; mCallbackText.setText("Disconnected."); // As part of the sample, tell the user what happened. Toast.makeText(Binding.this, R.string.remote_service_disconnected, Toast.LENGTH_SHORT).show(); &#125; &#125;; void doBindService() &#123; // Establish a connection with the service. We use an explicit // class name because there is no reason to be able to let other // applications replace our component. bindService(new Intent(Binding.this, MessengerService.class), mConnection, Context.BIND_AUTO_CREATE); mIsBound = true; mCallbackText.setText("Binding."); &#125; void doUnbindService() &#123; if (mIsBound) &#123; // If we have received the service, and hence registered with // it, then now is the time to unregister. if (mService != null) &#123; try &#123; Message msg = Message.obtain(null, MessengerService.MSG_UNREGISTER_CLIENT); msg.replyTo = mMessenger; mService.send(msg); &#125; catch (RemoteException e) &#123; // There is nothing special we need to do if the service // has crashed. &#125; &#125; // Detach our existing connection. unbindService(mConnection); mIsBound = false; mCallbackText.setText("Unbinding."); &#125; &#125;&#125; AIDL的使用AIDL是 Android Interface definition language的缩写，一看就明白，它是一种android内部进程通信接口的描述语言，通过它我们可以定义进程间的通信接口。对于AIDL的设计以及概念此处不做过多讲解。有问题自行Google查阅。使用步骤： 1.定义AIDL文件（包含接口和用到的实现了parcelable接口的实体）2.在服务中使用。 以下以一个使用了AIDL的音乐播放器进行示例的描述： 123// MAudioItem.aidl 歌曲对象实体类的AIDL映射package cn.jiajunhui.media.entity;parcelable MAudioItem; 注意，AIDL放置的包的目录一定要和实体类的包目录路径一致。 12345678910// IMusicListener.aidl 播放状态的监听器package cn.jiajunhui.media;// Declare any non-default types here with import statementsinterface IMusicListener &#123; void action(in int code, in Message message);&#125; 123456789101112131415161718192021// IMusicPlay.aidlpackage cn.jiajunhui.media;import cn.jiajunhui.media.IMusicListener;import cn.jiajunhui.media.entity.MAudioItem;// Declare any non-default types here with import statementsinterface IMusicPlay &#123; void action(in int code, in Bundle bundle); int getAudioSessionId(); int getCurrentPosition(); int getDuration(); int getState(); boolean isPlaying(); MAudioItem getCurrentItem(); void registerListener(in IMusicListener musicListener); void unregisterListener(in IMusicListener musicListener);&#125; 注意事项： 在AIDL中用到的对象类型必须要实现parcelable接口。如MAudioItem。 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162636465public class MAudioItem implements Parcelable, Serializable&#123; private int id; private String displayName; private String path; private long size; private long duration; private long modified; private String title; private String album; private String artist; private boolean checked; public MAudioItem() &#123; &#125; protected MAudioItem(Parcel in) &#123; id = in.readInt(); displayName = in.readString(); path = in.readString(); size = in.readLong(); duration = in.readLong(); modified = in.readLong(); title = in.readString(); album = in.readString(); artist = in.readString(); checked = in.readByte()!=0; &#125; public static final Creator&lt;MAudioItem&gt; CREATOR = new Creator&lt;MAudioItem&gt;() &#123; @Override public MAudioItem createFromParcel(Parcel in) &#123; return new MAudioItem(in); &#125; @Override public MAudioItem[] newArray(int size) &#123; return new MAudioItem[size]; &#125; &#125;; //......... // getter and setter method //......... @Override public int describeContents() &#123; return 0; &#125; @Override public void writeToParcel(Parcel dest, int flags) &#123; dest.writeInt(id); dest.writeString(displayName); dest.writeString(path); dest.writeLong(size); dest.writeLong(duration); dest.writeLong(modified); dest.writeString(title); dest.writeString(album); dest.writeString(artist); dest.writeByte((byte) (checked ? 1 : 0)); &#125;&#125; 注意：read和write的顺序一定一定要一致。还有，使用过程中我们会发现没有readBoolean和writeBoolean，这时我们需要特殊处理下，read时使用in.readByte()!=0;，write时使用dest.writeByte((byte) (checked ? 1 : 0)); 在服务中的使用： 1234567891011121314151617181920212223242526272829303132public class MusicService extends Service &#123; IMusicPlay mBinder = new IMusicPlay.Stub() &#123; @Override public void action(int code, Bundle bundle) throws RemoteException &#123; //.... &#125; //.... //implements other methods. &#125;; @Override public void onCreate() &#123; super.onCreate(); //.... &#125; @Nullable @Override public Binder onBind(Intent intent) &#123; return (Binder) mBinder; &#125; @Override public void onDestroy() &#123; super.onDestroy(); //.... &#125;&#125; 在Activity中进行绑定连接： 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647public class MusicPlayActivity extends Activity &#123; public void onCreate(Bundle saveInstanceState)&#123; bindMusicService(); &#125; private IMusicPlay mMusicPlay; private void bindMusicService() &#123; Intent serviceIntent = new Intent(this, MusicService.class); bindService(serviceIntent, mServiceConnection, BIND_AUTO_CREATE); &#125; private ServiceConnection mServiceConnection = new ServiceConnection() &#123; @Override public void onServiceConnected(ComponentName name, IBinder service) &#123; if(service!=null)&#123; try &#123; mMusicPlay = (IMusicPlay) service; mMusicPlay.registerListener(musicListener); //.... &#125; catch (RemoteException e) &#123; e.printStackTrace(); &#125; &#125; &#125; @Override public void onServiceDisconnected(ComponentName name) &#123; &#125; &#125;; private IMusicListener musicListener = new IMusicListener.Stub() &#123; @Override public void action(int code, Message message) throws RemoteException &#123; //.... &#125; &#125;; @Override protected void onDestroy() &#123; super.onDestroy(); //.... &#125; &#125; 关于服务的一些事项服务的整个生命周期概览官方的示例图： 服务和线程的区别Thread 是程序执行的最小单元，它是分配CPU的基本单位，android系统中UI线程也是线程的一种，当然Thread还可以用于执行一些耗时异步的操作。 Service是Android的一种机制，服务是运行在主线程上的，它是由系统进程托管。它与其他组件之间的通信类似于client和server，是一种轻量级的IPC通信，这种通信的载体是binder，它是在linux层交换信息的一种IPC，而所谓的Service后台任务只不过是指没有UI的组件罢了。 在android系统中，线程一般指的是工作线程(即后台线程)，而主线程是一种特殊的工作线程，它负责将事件分派给相应的用户界面小工具，如绘图事件及事件响应，因此为了保证应用 UI 的响应能力主线程上不可执行耗时操作。如果执行的操作不能很快完成，则应确保它们在单独的工作线程执行。 Service 则是android系统中的组件，一般情况下它运行于主线程中，因此在Service中是不可以执行耗时操作的，否则系统会报ANR异常，之所以称Service为后台服务，大部分原因是它本身没有UI，用户无法感知(当然也可以利用某些手段让用户知道)，但如果需要让Service执行耗时任务，可在Service中开启单独线程去执行。 当要执行耗时的网络或者数据库查询以及其他阻塞UI线程或密集使用CPU的任务时，都应该使用工作线程(Thread)，这样才能保证UI线程不被占用而影响用户体验。 在应用程序中，如果需要长时间的在后台运行，而且不需要交互的情况下，使用服务。比如播放音乐，通过Service+Notification方式在后台执行同时在通知栏显示着。 在大部分情况下，Thread和Service都会结合着使用，比如下载文件，一般会通过Service在后台执行+Notification在通知栏显示+Thread异步下载，再如应用程序会维持一个Service来从网络中获取推送服务。在Android官方看来也是如此，所以官网提供了一个Thread与Service的结合来方便我们执行后台耗时任务，它就是IntentService，(如果想更深入了解IntentService，可以看博主的另一篇文章：Android 多线程之IntentService 完全详解)，当然 IntentService并不适用于所有的场景，但它的优点是使用方便、代码简洁，不需要我们创建Service实例并同时也创建线程，某些场景下还是非常赞的！由于IntentService是单个worker thread，所以任务需要排队，因此不适合大多数的多任务情况。  abstract class IntentService extends Service &#123; private volatile Looper mServiceLooper; private volatile ServiceHandler mServiceHandler; private String mName; private boolean mRedelivery; private final class ServiceHandler extends Handler &#123; public ServiceHandler(Looper looper) &#123; super(looper); &#125; @Override public void handleMessage(Message msg) &#123; onHandleIntent((Intent)msg.obj); stopSelf(msg.arg1); &#125; &#125; /** * Creates an IntentService. Invoked by your subclass's constructor. * * @param name Used to name the worker thread, important only for debugging. */ public IntentService(String name) &#123; super(); mName = name; &#125; /** * Sets intent redelivery preferences. Usually called from the constructor * with your preferred semantics. * * &lt;p&gt;If enabled is true, * &#123;@link #onStartCommand(Intent, int, int)&#125; will return * &#123;@link Service#START_REDELIVER_INTENT&#125;, so if this process dies before * &#123;@link #onHandleIntent(Intent)&#125; returns, the process will be restarted * and the intent redelivered. If multiple Intents have been sent, only * the most recent one is guaranteed to be redelivered. * * &lt;p&gt;If enabled is false (the default), * &#123;@link #onStartCommand(Intent, int, int)&#125; will return * &#123;@link Service#START_NOT_STICKY&#125;, and if the process dies, the Intent * dies along with it. */ public void setIntentRedelivery(boolean enabled) &#123; mRedelivery = enabled; &#125; @Override public void onCreate() &#123; // TODO: It would be nice to have an option to hold a partial wakelock // during processing, and to have a static startService(Context, Intent) // method that would launch the service &amp; hand off a wakelock. super.onCreate(); HandlerThread thread = new HandlerThread("IntentService[" + mName + "]"); thread.start(); mServiceLooper = thread.getLooper(); mServiceHandler = new ServiceHandler(mServiceLooper); &#125; @Override public void onStart(@Nullable Intent intent, int startId) &#123; Message msg = mServiceHandler.obtainMessage(); msg.arg1 = startId; msg.obj = intent; mServiceHandler.sendMessage(msg); &#125; /** * You should not override this method for your IntentService. Instead, * override &#123;@link #onHandleIntent&#125;, which the system calls when the IntentService * receives a start request. * @see android.app.Service#onStartCommand */ @Override public int onStartCommand(@Nullable Intent intent, int flags, int startId) &#123; onStart(intent, startId); return mRedelivery ? START_REDELIVER_INTENT : START_NOT_STICKY; &#125; @Override public void onDestroy() &#123; mServiceLooper.quit(); &#125; /** * Unless you provide binding for your service, you don't need to implement this * method, because the default implementation returns null. * @see android.app.Service#onBind */ @Override @Nullable public IBinder onBind(Intent intent) &#123; return null; &#125; /** * This method is invoked on the worker thread with a request to process. * Only one Intent is processed at a time, but the processing happens on a * worker thread that runs independently from other application logic. * So, if this code takes a long time, it will hold up other requests to * the same IntentService, but it will not hold up anything else. * When all requests have been handled, the IntentService stops itself, * so you should not call &#123;@link #stopSelf&#125;. * * @param intent The value passed to &#123;@link * android.content.Context#startService(Intent)&#125;. * This may be null if the service is being restarted after * its process has gone away; see * &#123;@link android.app.Service#onStartCommand&#125; * for details. */ @WorkerThread protected abstract void onHandleIntent(@Nullable Intent intent);&#125; 源码其实只有短短的100来行。那么IntentService是用来干嘛的呢？看源码我们会发现，其实在IntentService创建时，创建了一个HandlerThread对象，对于HandlerThread不明白的，请自行Google。IntentService是继承自Service的一个抽象类，void onHandleIntent(@Nullable Intent intent);为抽象的方法，追根溯源不难发现其最终和HandlerThread关联到了一起，其实IntentService设计的目的就是为了可以在Service中处理耗时的任务，因为Service本身是运行于主线程的，并不能处理耗时的任务。 通过源码可以看到当onHandleIntent执行完后，调用了selfStop方法，自杀了。这其实也是IntentService的特色之处，干完活就over，不拖泥带水。 我们可以通过IntentService来实现一个应用的下载功能，下载完就死了，及时销毁，不浪费资源。使用便捷。 关于Service就先讲这么多。]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Service</tag>
        <tag>IntentService</tag>
        <tag>Messenger</tag>
        <tag>Binder</tag>
        <tag>AIDL</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[MediaLoader-用于加载Android系统的Media资源]]></title>
    <url>%2Fproject_mediaload%2F</url>
    <content type="text"><![CDATA[项目介绍项目开发过程中需要做一个选取图片的界面，也就是要读取系统的所有相册图片展示出来，让用户去选取，然后进行上传或者裁切之类的。通过系统提供的Uri可以读取到，但是自己需要写一堆查询设置，然后取到结果后又要处理一堆cursor，处理起来相当头大。但是还是硬着头皮给写了，项目完工后把这块整理封装了下。如果项目中再用到，使用上就相当方便，而且我加入了对系统视频资源、音频资源的加载，而且可以进行一定的自定义设置过滤。项目已传到github且上传到了jcenter，可一行代码引入。 项目github地址：MediaLoader jcenter依赖:1compile 'com.jiajunhui.xapp.medialoader:medialoader:1.0.0' 功能介绍与用法可直接加载系统内的图片资源、视频资源、音频资源或者其他类型资源（如apk文件，zip文件，doc文件），下面进行详细介绍： 注：targetApi版本为23或以上的，请注意运行时权限（Runtime Permission） 加载图片资源1234567MediaLoader.getLoader().loadPhotos(this, new OnPhotoLoaderCallBack() &#123; @Override public void onResult(PhotoResult result) &#123; List&lt;PhotoFolder&gt; folders = result.getFolders(); List&lt;PhotoItem&gt; items = result.getItems(); &#125;&#125;); 加载视频资源1234567MediaLoader.getLoader().loadVideos(this, new OnVideoLoaderCallBack() &#123; @Override public void onResult(VideoResult result) &#123; List&lt;VideoFolder&gt; folders = result.getFolders(); List&lt;VideoItem&gt; items = result.getItems(); &#125;&#125;); 加载音频资源123456MediaLoader.getLoader().loadAudios(this, new OnAudioLoaderCallBack() &#123; @Override public void onResult(AudioResult result) &#123; List&lt;AudioItem&gt; items = result.getItems(); &#125;&#125;); 加载zip、doc、apk资源加载zip资源123456MediaLoader.getLoader().loadFiles(MainActivity.this, new OnFileLoaderCallBack(FileType.ZIP) &#123; @Override public void onResult(FileResult result) &#123; List&lt;FileItem&gt; items = result.getItems(); &#125;&#125;); 加载doc资源123456MediaLoader.getLoader().loadFiles(MainActivity.this, new OnFileLoaderCallBack(FileType.DOC) &#123; @Override public void onResult(FileResult result) &#123; &#125;&#125;); 加载apk安装包123456MediaLoader.getLoader().loadFiles(MainActivity.this, new OnFileLoaderCallBack(FileType.APK) &#123; @Override public void onResult(FileResult result) &#123; &#125;&#125;); 自定义加载通过重写查询时需要的字段、查询条件以及对应条件的参数以及自定义排序等等，进行自定义的查询并处理返回结果。 1234567891011121314151617181920212223242526MediaLoader.getLoader().loadFiles(MainActivity.this, new OnFileLoaderCallBack()&#123; @Override public String getSortOrderSql() &#123; return super.getSortOrderSql(); &#125; @Override public Uri getQueryUri() &#123; return super.getQueryUri(); &#125; @Override public String getSelections() &#123; return super.getSelections(); &#125; @Override public String[] getSelectProjection() &#123; return super.getSelectProjection(); &#125; @Override public String[] getSelectionsArgs() &#123; return super.getSelectionsArgs(); &#125; @Override public void onResult(FileResult result) &#123; &#125;&#125;); 总结媒体库资源的加载使用的是系统提供的CursorLoader，对于CursorLoader此处不做过多讲解。MediaLoader库是简化了我们查询时的调用以及处理。 对源码感兴趣的朋友可以去我的github上clone一份代码详细查看。]]></content>
      <categories>
        <category>我的开源项目</category>
      </categories>
      <tags>
        <tag>Android</tag>
        <tag>媒体库</tag>
        <tag>CursorLoader</tag>
        <tag>Media</tag>
        <tag>Video</tag>
        <tag>Music</tag>
        <tag>Audio</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[自定义控件01-电池图标]]></title>
    <url>%2Fandroid_view_battery%2F</url>
    <content type="text"><![CDATA[介绍开启自定义控件篇章，首先来画个电池图标。我们在用一些播放器全屏观看视频时能看到顶栏上有个小电池图标，很是逼真，今天就带领大家实现以下。先看看效果： 开始添加自定义属性在values下的attrs.xml（如文件不存在新建一个）重定义控件需要的属性。直观的去看电池图标，基本可以定义出它需要的属性。图标边框粗细、边框颜色、剩余电量进度颜色、数字字体大小、电量显示类型、电池正极的朝向。 1234567891011121314151617181920212223&lt;?xml version="1.0" encoding="utf-8"?&gt;&lt;resources&gt; &lt;declare-styleable name="BatteryView"&gt; &lt;!--边框宽度--&gt; &lt;attr name="border_width" format="dimension"/&gt; &lt;!--边框颜色--&gt; &lt;attr name="border_color" format="color|reference"/&gt; &lt;!--数字字体大小--&gt; &lt;attr name="number_text_size" format="dimension"/&gt; &lt;!--电池余量进度颜色--&gt; &lt;attr name="progress_color" format="color|reference"/&gt; &lt;!--电量显示类型--&gt; &lt;attr name="show_type"&gt; &lt;enum name="fill" value="0"/&gt; &lt;enum name="number" value="1"/&gt; &lt;/attr&gt; &lt;!--正极朝向--&gt; &lt;attr name="head_orientation"&gt; &lt;flag name="left" value="0"/&gt; &lt;flag name="right" value="2"/&gt; &lt;/attr&gt; &lt;/declare-styleable&gt;&lt;/resources&gt; 创建继承自View的类BatteryView首先就是重载那几个构造方法，接下来就是解析属性值了。 123456789101112131415@Overridepublic BatteryView(Context context, AttributeSet attrs, int defStyleAttr) &#123; super(context, attrs, defStyleAttr); TypedArray typedArray = context.obtainStyledAttributes(attrs, R.styleable.BatteryView); if (typedArray != null) &#123; mBorderColor = typedArray.getColor(R.styleable.BatteryView_border_color, DEFAULT_BORDER_COLOR); mProgressColor = typedArray.getColor(R.styleable.BatteryView_progress_color, DEFAULT_BORDER_COLOR); mBorderWidth = (int) typedArray.getDimension(R.styleable.BatteryView_border_width, DEFAULT_BORDER_WIDTH); mNumberTextSize = (int) typedArray.getDimension(R.styleable.BatteryView_number_text_size, DEFAULT_NUMBER_TEXT_SIZE); mHeadOrientation = typedArray.getInt(R.styleable.BatteryView_head_orientation, HEAD_ORIENTATION_LEFT); mShowType = typedArray.getInt(R.styleable.BatteryView_show_type, SHOW_TYPE_FILL); typedArray.recycle(); &#125; init(context);&#125; 接下来就是对控件的测量进行处理。当测量模式为未指定或者最大时对宽高进行默认最小值处理。如下： 123456789101112131415@Overrideprotected void onMeasure(int widthMeasureSpec, int heightMeasureSpec) &#123; int specModeW = MeasureSpec.getMode(widthMeasureSpec); int specModeH = MeasureSpec.getMode(heightMeasureSpec); int width; int height; if(specModeW == MeasureSpec.AT_MOST || specModeH == MeasureSpec.AT_MOST || specModeW == MeasureSpec.UNSPECIFIED || specModeH == MeasureSpec.UNSPECIFIED) &#123; width = MIN_WIDTH; height = MIN_HEIGHT; setMeasuredDimension(width,height); &#125;else &#123; super.onMeasure(widthMeasureSpec, heightMeasureSpec); &#125;&#125; 接下来就是关键部分了，开始画了。分为了两部分，画外框和内部电量显示。 代码如下： 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152535455565758596061626364656667private void drawBorder(Canvas canvas) &#123; int[] headRect = new int[4]; int[] bodyRect = new int[4]; if(mHeadOrientation == HEAD_ORIENTATION_LEFT)&#123; headRect[0] = 0; headRect[1] = mHeight/4; headRect[2] = HEAD_WIDTH; headRect[3] = mHeight*3/4; bodyRect[0] = HEAD_WIDTH; bodyRect[1] = 0; bodyRect[2] = mWidth; bodyRect[3] = mHeight; &#125;else if(mHeadOrientation == HEAD_ORIENTATION_RIGHT)&#123; headRect[0] = mWidth - HEAD_WIDTH; headRect[1] = mHeight/4; headRect[2] = mWidth; headRect[3] = mHeight*3/4; bodyRect[0] = 0; bodyRect[1] = 0; bodyRect[2] = mWidth - HEAD_WIDTH; bodyRect[3] = mHeight; &#125; mBorderPaint.setStyle(Paint.Style.FILL); canvas.drawRect(new RectF(headRect[0],headRect[1],headRect[2],headRect[3]),mBorderPaint); mBorderPaint.setStyle(Paint.Style.STROKE); Rect rect = new Rect(bodyRect[0], bodyRect[1], bodyRect[2], bodyRect[3]); mBodyWidth = rect.width(); canvas.drawRect(rect,mBorderPaint);&#125;private void drawProgress(Canvas canvas) &#123; if(mProgress&lt;=mLowPowerValue &amp;&amp; isLowPowerWarn)&#123; mProgressPaint.setColor(mLowPowerProgressColor); &#125;else&#123; mProgressPaint.setColor(mProgressColor); &#125; if(mShowType == SHOW_TYPE_FILL)&#123; int[] rectF = new int[4]; int progressW = getProgressWidth(mProgress); if(mHeadOrientation == HEAD_ORIENTATION_LEFT)&#123; rectF[0] = mWidth - progressW - mBorderWidth; rectF[1] = mBorderWidth; rectF[2] = mWidth - mBorderWidth; rectF[3] = mHeight - mBorderWidth; &#125;else if(mHeadOrientation == HEAD_ORIENTATION_RIGHT)&#123; rectF[0] = mBorderWidth; rectF[1] = mBorderWidth; rectF[2] = mBorderWidth + progressW; rectF[3] = mHeight - mBorderWidth; &#125; canvas.drawRect(new RectF(rectF[0],rectF[1],rectF[2],rectF[3]),mProgressPaint); &#125;else if(mShowType == SHOW_TYPE_NUMBER)&#123; String progressText = String.valueOf(mProgress); mProgressPaint.setTextAlign(Paint.Align.LEFT); Rect bounds = new Rect(); mProgressPaint.getTextBounds(progressText, 0, progressText.length(), bounds); Paint.FontMetricsInt fontMetrics = mProgressPaint.getFontMetricsInt(); int baseline = (getMeasuredHeight() - fontMetrics.bottom + fontMetrics.top) / 2 - fontMetrics.top; canvas.drawText(progressText,mBodyWidth / 2 - bounds.width() / 2, baseline, mProgressPaint); &#125;&#125;private int getProgressWidth(int progress)&#123; return (int) (((mWidth - HEAD_WIDTH - (2*mBorderWidth))*1.0/100)*progress);&#125; 其中为了更接近真实的使用场景，加入了低电量高亮显示的预警提示，如电量低于20%时电池余量显示红色。 完整代码见：BatteryView]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>自定义控件</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[Android组件之Activity]]></title>
    <url>%2Fandroid_activity%2F</url>
    <content type="text"><![CDATA[前言Activity 是一个重要的应用组件，用户可与其提供的屏幕进行交互，以执行拨打电话、拍摄照片、发送电子邮件或查看地图等操作。 每个 Activity 都会获得一个用于绘制其用户界面的窗口Window。窗口通常会充满屏幕，但也可小于屏幕并浮动在其他窗口之上。一个应用通常由多个彼此松散联系的 Activity 组成。 一般会指定应用中的某个 Activity 为“主”Activity，即首次启动应用时呈现给用户的那个 Activity（也成为launcher Activity）。 而且每个 Activity 均可启动另一个 Activity，进入其他活动页面，以便执行不同的操作。 Activity（页面/活动）创建Activity要创建一个Activity，我们需要继承自Activity或者其现有的子类（FragmentActivity或AppcompatActivity），在onCreate方法中进行布局的设置以及其他的初始化操作，接下来根据需要一般还要实现Activity的多个由系统调用的生命周期方法。 接下来需要在清单文件中对创建的Activity进行声明和配置。需要在AndroidManifest.xml下的application节点下创建一个Activity节点并补充其name属性（必须）和其他属性。 1234567&lt;manifest ... &gt; &lt;application ... &gt; &lt;activity android:name=".DemoActivity" /&gt; ... &lt;/application ... &gt; ...&lt;/manifest &gt; 在Activity节点下配置intent过滤器intent过滤器是在Activity节点下通过intent-filter标签进行配置的以声明如何激活该Activity的方法。 123456&lt;activity android:name=".DemoActivity" android:icon="@drawable/icon_test"&gt; &lt;intent-filter&gt; &lt;action android:name="android.intent.action.MAIN" /&gt; &lt;category android:name="android.intent.category.LAUNCHER" /&gt; &lt;/intent-filter&gt;&lt;/activity&gt; 此处action 元素指定这是应用的“主”入口点。category 元素指定此 Activity 应列入系统的应用启动器内（以便用户启动该 Activity）。如果您打算让应用成为独立应用，不允许其他应用激活其 Activity，则您不需要任何其他 Intent 过滤器。 正如前例所示，只应有一个 Activity 具有“主”操作和“launcher”类别。 您不想提供给其他应用的 Activity 不应有任何 Intent 过滤器，您可以利用显式 Intent 自行启动它们，但是，如果您想让 Activity 对衍生自其他应用（以及您的自有应用）的隐式 Intent 作出响应，则必须为 Activity 定义其他 Intent 过滤器。 对于您想要作出响应的每一个 Intent 类型，您都必须加入相应的 intent-filter，其中包括一个 action 元素，还可选择性地包括一个 category 元素和/或一个data 元素。这些元素指定您的 Activity 可以响应的 Intent 类型。 Activity的启动以及启动模式我们通过intent来描述我们将要启动的Activity，并将intent传递给startActivity方法。Intent可以指定您想启动的具体 Activity 或描述您想执行的操作类型（比如你设置打开类型为视频文件，系统会帮你弹出能播放视频的应用，系统会为您选择合适的 Activity或应用）。 Intent 对象还可能携带少量供所启动 Activity 使用的数据。比如我们打开一个本地的视频文件，通过intent将视频路径信息传递出去。 12345Intent intent = new Intent(Intent.ACTION_VIEW);String type = "video/mp4";Uri uri = Uri.parse("file:///sdcard/test.mp4");intent.setDataAndType(uri, type);startActivity(intent); 在应用内，一般我们启动的都是自己创建的名称已知的Activity，我们直接可以通过Activity类名显式定义intent来启动指定的Activity。 比如应用内有个MainActivity，使用如下启动方式： 12Intent intent = new Intent(this, MainActivity.class);startActivity(intent); 有时我们启动Activity后需要得到被启动的Activity的回执结果，这种情况就需要另一个方法来启动Activity了，通过startActivityForResult()方法，被启动的Activity通过setResult将加过回执，我们需要通过onActivityResult回调方法来接收回执信息。 1234567891011//....Intent intent = new Intent(this, TestActivity.class);startActivityForResult(intent, TEST_REQUEST_CODE);//...@Overrideprotected void onActivityResult(int requestCode, int resultCode, Intent data) &#123; if(resultCode == Activity.RESULT_OK &amp;&amp; requestCode == TEST_REQUEST_CODE)&#123; //... &#125;&#125; 启动模式Activity的启动模式是为了更好的管理Activity儿设计的。Android采用Task来管理多个Activity，当我们启动一个应用时，Android就会为之创建一个Task，然后启动这个应用的入口Activity（即intent-filter标签中配置为 MAIN和LAUNCHER的Activity）。因为Android并没有为Task提供API，因此我们无法真正去访问Task，只能调用Activity的getTaskId()方法来获取它所在的Task的ID。事实上我们可以把Task理解成Activity栈，Task以栈的形式来管理Activity：先启动的Activity被放在Task栈底，后启动的Activity被放在Task栈顶。那么Activity的加载模式，就负责管理实例化、加载Activity的方式、并可以控制Activity与Task之间的关系。 Activity有4中启动模式，分别为：standard,singleTop,singleTask,singleInstance。如果要使用这四种启动模式，须在manifest文件中activity标签中的launchMode属性配置，如： 123456&lt;activity android:name=".TestActivity" android:launchMode="standard" &gt; //...&lt;/activity&gt; standard（标准启动模式）标准启动模式，也是Android的默认启动模式。在这种模式下启动的activity可以被多次实例化，即在一个任务中可以存在多个activity实例，每个实例都会处理一个Intent对象。如果Activity A的启动模式为standard，并且A已经启动，在A中再次启动Activity A，即调用startActivity(),会在A的上面再次启动一个A的实例，即当前的栈中的状态为A-&gt;A。 singleTop（栈顶复用模式）如果一个以singleTop模式启动的activity的实例已经存在于任务栈的栈顶，那么再次启动这个activity时，不会创建新的实例，而是重用位于栈顶的那个实例，并且会调用该实例的onNewIntent()方法将Intent对象传递到这个实例中。举例来说，如果A的启动模式为singleTop，并且A的一个实例已经存在于栈顶中，那么再调用startActivity()启动A时，不会再创建A的实例，而是重用原来的实例，并且调用原来实例的onNewIntent()方法。这时任务栈中还是有一个A的实例。如果以singleTop模式启动的activity的一个实例已经存在于任务栈中，但是不在栈顶，那么它的行为和standard模式相同，也会创建多个实例。使用场景：singleTop适合接收通知启动的内容显示页面（从外界可能多次跳转到一个界面）。例如，某个新闻客户端的新闻内容页面，如果收到10个新闻推送，每次激活同一个新闻内容页面，返回时立刻返回前一个页面。 singleTask（栈内复用模式）如果一个activity的启动模式为singleTask,那么系统总会在一个新的任务的最底部（root）启动这个activity，并且被这个activity启动的其它activity会和该activity同时存在于这个新任务中。如果系统中已经存在一个这样的activity则会重用这个实例，并且调用它的onNewIntent()方法。即，这样的一个activity在系统中只会存在一个实例。但是这种说法并不准确，采用singleTask启动目标Activity时，可分三种情况：1、如果将要开启的目标Activity不存在，系统将会创建目标Activity实例，并将它放入Task栈顶。2、如果将要启动的目标Activity已经位于Task栈顶，此时与singleTop模式的行为相同。3、如果将要启动的目标Activity已经存在、但没有位于Task栈顶，系统将会把位于该Activity上面的所有Activity移出Task栈，从而使得目标Activity转入栈顶。使用场景：适合作为程序的主页面。 singleInstance（单利模式）总是在新的任务中开启，并且这个新的任务中有且只有这一个实例，也就是说被该实例启动的其他activity会自动运行于另一个任务中。当再次启动该activity的实例时，会重用已存在的任务和实例。并且会调用这个实例的onNewIntent()方法，将Intent实例传递到该实例中。和singleTask相同，同一时刻在系统中只会存在一个这样的Activity实例。说明：这种加载模式下，系统保证无论从哪个Task中启动目标Activity，只会创建一个目标Activity实例，并会使用一个全新的Task栈来装载该Activity实例。当用singleInstance启动目标Activity时，分两种情况：1、当将要启动的目标Activity不存在，系统会先创建一个全新的Task、再创建目标Activity的实例，并将它加入新的Task的栈顶。2、如果将要启动的目标Activity已经存在，无论它位于哪个应用程序中，无论它位于哪个Task中，系统将会把该Activity所在的Task转到前台，从而使用该Activity显示出来。采用该模式加载的Activity总是位于Task栈顶，所在Task只包含该Activity.使用场景：闹铃的响铃界面。 你以前设置了一个闹铃：上午6点。在上午5点58分，你启动了闹铃设置界面，并按 Home 键回桌面；在上午5点59分时，你在微信和朋友聊天；在6点时，闹铃响了，并且弹出了一个对话框形式的 Activity(名为 AlarmAlertActivity) 提示你到6点了(这个 Activity 就是以 SingleInstance 加载模式打开的)，你按返回键，回到的是微信的聊天界面，这是因为 AlarmAlertActivity 所在的 Task 的栈只有他一个元素， 因此退出之后这个 Task 的栈空了。如果是以 SingleTask 打开 AlarmAlertActivity，那么当闹铃响了的时候，按返回键应该进入闹铃设置界面。 Activity的生命周期先看一张经典的图片 onCreate()首次创建 Activity 时调用。 您应该在此方法中执行所有正常的静态设置 — 创建视图、将数据绑定到列表等等。 系统向此方法传递一个 Bundle 对象，其中包含 Activity 的上一状态，不过前提是捕获了该状态（下文讲解）。该方法执行完成后执行onStart()方法。 onRestart()在 Activity 已停止并即将再次启动前调用。执行完后执行onStart()方法。 onStart()在 Activity 即将对用户可见之前调用。后面执行 onResume()方法。 onResume()在 Activity 即将开始与用户进行交互之前调用。 此时，Activity 处于 Activity 堆栈的顶层，并具有用户输入焦点。页面失去焦点执行onPause()。 onPause()当系统即将开始继续另一个Activity时调用。此方法通常用于确认对持久性数据的未保存更改、停止动画以及其他可能消耗CPU的内容，诸如此类。它应该非常迅速地执行所需操作，因为它返回后，下一个Activity 才能继续执行。如果 Activity 返回前台，则后接 onResume()，如果 Activity 转入对用户不可见状态，则后接onStop()。 onStop()在Activity对用户不再可见时调用。如果Activity被销毁，或另一个Activity（一个现有Activity或新 Activity）继续执行并将其覆盖，就可能发生这种情况。如果 Activity 恢复与用户的交互，则后接 onRestart()，如果 Activity 被销毁，则后接onDestroy()。 onDestroy()在Activity被销毁前调用。这是Activity将收到的最后调用。当Activity结束（有人对Activity调用了finish()），或系统为节省空间而暂时销毁该Activity实例时，可能会调用它。您可以通过 isFinishing()方法区分这两种情形。 保存Activity的状态以及恢复状态我们在应用开发中，经常会遇到页面需要保存一些状态的需求，那么这些操作放到Activity生命周期中的那个方法合适呢？我们先看一张表：“Killable”表示当前activity是否可以被杀死，意思是说当上面标记为Killable的方法返回之后，activity就可能随时被杀死。从表中不难看出在onPause方法调用完之前，activity都是不能够被杀死的，而onStop()和onDestroy()都是可以被杀死的。但是图中又标出了一个黄色的标记：HONEYCOMB。 官方文章原文是这样说的：Starting with Honeycomb, an application is not in the killable state until itsonStop() has returned.从Honeycomb(Android 3.0)开始，应用只有等到onStop()方法返回之后，才可以被杀死，也就是说在执行完onStop()方法之前，应用都不可能被杀死。 you should use the onPause() method to write any persistent data (such as user edits) to storage.你应该在onPause()方法中去存储那些持久性的数据，比如用户的输入等。 the method onSaveInstanceState(Bundle) is called before placing the activity in such a background state, allowing you to save away any dynamic instance state in your activity into the given Bundle, to be later received in onCreate(Bundle) if the activity needs to be re-created.onSaveInstanceState(Bundle)将在activity转入“background state后台状态”之前被调用，能让我们存储一些activity的动态的状态值到Bundle对象中，以便在之后调用onCreate(Bundle)方法时用到。 这里提到一个“background state后台状态”，我们来看看activity的几种状态：前台状态：通俗的说就是可以看到，且可以操作(有焦点)的状态 可视状态：即可以看得见(没有被完全遮挡)，但是没有焦点，不可以触摸操作；比如躲在对话框后面的activity 后台状态：已经看不到了，系统可以将这个进程杀死来回收内存。如果在这种状态下activity被系统杀死了，那么在用户重新打开这个activity的时候，它的onCreate方法会使用之前onSaveInstanceState(Bundle)保存的状态数据，来让自己恢复到之前的状态 空进程状态：一个没有持有任何activity和任何应用组件的进程，比如Services或者广播接受者，当内存不足的时候，它们将会被先杀死并回收。 也就是说onSaveInstanceState(Bundle)会在activity转入后台状态之前被调用，也就是onStop()方法之前，onPause方法之后被调用；我们都知道在默认情况下，在旋屏之后，activity会重新经历一次生命周期，下面的log就是在点击旋屏之后的执行顺序： Fragment（碎片）Fragment是Android3.0后引入的一个新的API，他出现的初衷是为了适应大屏幕的平板电脑， 当然现在他仍然是平板APP UI设计的宠儿，而且我们普通手机开发也会加入这个Fragment， 我们可以把他看成一个小型的Activity，又称Activity片段！想想，如果一个很大的界面，我们 就一个布局，写起界面来会有多麻烦，而且如果组件多的话是管理起来也很麻烦！而使用Fragment 我们可以把屏幕划分成几块，然后进行分组，进行一个模块化的管理！从而可以更加方便的在 运行过程中动态地更新Activity的用户界面！另外Fragment并不能单独使用，他需要嵌套在Activity 中使用，尽管他拥有自己的生命周期，但是还是会受到宿主Activity的生命周期的影响，比如Activity 被destory销毁了，他也会跟着销毁！ 先看下声明周期： 主要方法setUserVisibleHint()：设置Fragment可见或者不可见时会调用此方法。在该方法里面可以通过调用getUserVisibleHint()获得Fragment的状态是可见还是不可见的，如果可见则进行懒加载操作。onAttach()：执行该方法时，Fragment与Activity已经完成绑定，该方法有一个Activity类型的参数，代表绑定的Activity，这时候你可以执行诸如mActivity = activity的操作。onCreate()：初始化Fragment。可通过参数savedInstanceState获取之前保存的值。onCreateView()：初始化Fragment的布局。加载布局和findViewById的操作通常在此函数内完成，但是不建议执行耗时的操作，比如读取数据库数据列表。onActivityCreated()：执行该方法时，与Fragment绑定的Activity的onCreate方法已经执行完成并返回，在该方法内可以进行与Activity交互的UI操作，所以在该方法之前Activity的onCreate方法并未执行完成，如果提前进行交互操作，会引发空指针异常。onStart()：执行该方法时，Fragment由不可见变为可见状态。onResume()：执行该方法时，Fragment处于活动状态，用户可与之交互。onPause()：执行该方法时，Fragment处于暂停状态，但依然可见，用户不能与之交互。onSaveInstanceState()：保存当前Fragment的状态。该方法会自动保存Fragment的状态，比如EditText键入的文本，即使Fragment被回收又重新创建，一样能恢复EditText之前键入的文本。onStop()：执行该方法时，Fragment完全不可见。onHiddenChanged:当我们通过FragmentTransaction调用hide()和show()方法时会被调用。onDestroyView()：销毁与Fragment有关的视图，但未与Activity解除绑定，依然可以通过onCreateView方法重新创建视图。通常在ViewPager+Fragment的方式下会调用此方法。onDestroy()：销毁Fragment。通常按Back键退出或者Fragment被回收时调用此方法。onDetach()：解除与Activity的绑定。在onDestroy方法之后调用。 主要方法执行流程Fragment创建：setUserVisibleHint()-&gt;onAttach()-&gt;onCreate()-&gt;onCreateView()-&gt;onActivityCreated()-&gt;onStart()-&gt;onResume()； Fragment变为不可见状态（锁屏、回到桌面、被Activity完全覆盖）：onPause()-&gt;onSaveInstanceState()-&gt;onStop()； Fragment变为部分可见状态（打开Dialog样式的Activity）：onPause()-&gt;onSaveInstanceState()； Fragment由不可见变为活动状态：onStart()-&gt;OnResume()； Fragment由部分可见变为活动状态：onResume()； 退出应用：onPause()-&gt;onStop()-&gt;onDestroyView()-&gt;onDestroy()-&gt;onDetach()（注意退出不会调用onSaveInstanceState方法，因为是人为退出，没有必要再保存数据）； Fragment被回收又重新创建：被回收执行onPause()-&gt;onSaveInstanceState()-&gt;onStop()-&gt;onDestroyView()-&gt;onDestroy()-&gt;onDetach()，重新创建执行onAttach()-&gt;onCreate()-&gt;onCreateView()-&gt;onActivityCreated()-&gt;onStart()-&gt;onResume()-&gt;setUserVisibleHint()； 简单使用123456789101112131415public class TestFragment extends Fragment&#123; public static TestFragment getInstance(String param, String tag)&#123; TestFragment fragment = new TestFragment(); Bundle bundle = new Bundle(); bundle.putString("param", param); fragment.setArguments(bundle); return fragment; &#125; public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) &#123; return inflater.inflate(R.layout.item_test_visibility, container, false); &#125;&#125; 常见问题（1）Fragment的懒加载问题这个问题需要结合上文中提到的几个方法来解决，onHiddenChanged()和setUserVisibleHint()。我们可以这么做： 123456789101112131415161718192021222324252627282930313233@Overridepublic void onViewCreated(View view, @Nullable Bundle savedInstanceState) &#123; super.onViewCreated(view, savedInstanceState); if(!isInit)&#123; isInit = true; initViews(); onInitData(); &#125;&#125;@Overridepublic void setUserVisibleHint(boolean isVisibleToUser) &#123; super.setUserVisibleHint(isVisibleToUser); if(isInit)&#123; if(isVisibleToUser)&#123; onVisible(); &#125;else&#123; onInVisible(); &#125; &#125;&#125;@Overridepublic void onHiddenChanged(boolean hidden) &#123; super.onHiddenChanged(hidden); if(isInit)&#123; if(hidden)&#123; onInVisible(); &#125;else&#123; onVisible(); &#125; &#125;&#125; 在onVisible中可以做我们想延时加载的任务。 （2）Fragment的onCreateView重新初始化问题。当我们使用ViewPager来管理多个Fragment时会出现这个问题，由于ViewPager会对Fragment进行缓存，所以当重新呈现时会重新执行onCreateView方法，所以需要在onCreateView时做如下处理： 123456789101112@Overridepublic View onCreateView(LayoutInflater inflater, @Nullable ViewGroup container, @Nullable Bundle savedInstanceState) &#123; if(mRootView==null)&#123; mRootView = getContentView(inflater, savedInstanceState); &#125;else&#123; ViewGroup parent = (ViewGroup) mRootView.getParent(); if (parent != null)&#123; parent.removeView(mRootView); &#125; &#125; return mRootView;&#125;]]></content>
      <categories>
        <category>Android</category>
      </categories>
      <tags>
        <tag>Activity</tag>
        <tag>Fragment</tag>
      </tags>
  </entry>
</search>
